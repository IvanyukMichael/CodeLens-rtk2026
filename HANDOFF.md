# CodeLens — HANDOFF (передача проекта команде)

> Этот файл — карта репозитория и точка входа для всех, кто продолжает работу.
> Прочитай его целиком перед тем, как что-то менять. Здесь: что уже сделано,
> что делает каждый файл, какие решения приняты и почему, что осталось,
> и где разложены грабли. Технический контекст для AI-агента — в [CLAUDE.md](CLAUDE.md),
> метрики и обоснования — в [REPORT.md](REPORT.md), продукт и запуск — в [README.md](README.md).

**Статус на момент передачи:** рабочий продукт + полная документация + research report.
Рабочее дерево чистое, всё закоммичено (последний коммит — «Отчет»).
**Финальная метрика:** Precision@5 = **0.800** (офиц. 15) / **0.831** (расш. 71), детерминирована.
**Что НЕ закрыто:** база ≥80 файлов, LLM-чат, Docker, демо-репетиция (см. §6).
(Гигиена сдачи закрыта 2026-06-13: `results.json` пересобран финальной конфигурацией —
Total P@5 = 0.800; `__pycache__` убран из индекса git.)

---

## 1. Что это и где мы в проекте

CodeLens — RAG-система семантического поиска по Python-коду (Кейс 3, 2-й этап чемпионата
ГК «Ростелеком», дедлайн 24.06.2026). Вопрос на естественном языке (RU/EN) → top-5
релевантных фрагментов кода с подсветкой, релевантностью и chunk_id.

**Финальная конфигурация ретрива (заморожена):**
`bge-m3` + enriched (в базе) + HyDE-mix (α=0.5) на стороне запроса, HyDE `temperature=0`
(greedy, детерминирован), хранилище ChromaDB.

**Путь, который прошли** (числа — на расширенном eval из 71 вопроса, это авторитетный набор;
вся история — в [REPORT.md](REPORT.md)):

```
MiniLM raw 0.556 → bge-m3 raw 0.730 → +enriched 0.782 → +HyDE-mix 0.822 (frozen temp=0.2)
                                                       → live temp=0:  0.831 (71) / 0.800 (офиц.15)  ← ПРОД
```
(для справки: baseline MiniLM на официальных 15 вопросах = 0.700.)

**Отклонено по данным** (на 71 вопросе, с парным бутстрапом):
- **rerank** (cross-encoder) — в пределах шума (Δ=+0.038 vs enriched, CI пересекает 0); стоит
  модель 568M + латентность, надёжного выигрыша нет → не внедрён;
- **BM25-гибрид (RRF)** — оверфит: 0.833 только на официальном срезе, на held-out расширенном
  падает до 0.768, total = 0.782 (теряет весь выигрыш HyDE) → не внедрять;
- **multi-HyDE** (×3 сэмпла) — нулевой прирост (0.819 vs 0.822), 3× дороже → одиночный HyDE.

Единственный **статистически значимый** прирост — enrichment (Δ=+0.052, 95% CI [+0.012, +0.099],
6 вопросов лучше / 0 хуже). HyDE-mix направленно реален, но при n=71 ещё не значим (CI [−0.016, +0.101]).

---

## 2. Карта репозитория

### 2.1 Продакшн-код (несущая часть, работает сейчас)

| Файл | Что делает | Трогать осторожно? |
|---|---|---|
| [extract_chunks.py](extract_chunks.py) | AST-экстрактор. Режет `.py` на функции/классы/методы. `chunk_id = path:name:line`, для метода — `ClassName.method`. Ручная рекурсия `iter_child_nodes` (НЕ `ast.walk`), сигнатура через `ast.unparse`. `extract_repo()` обходит весь репозиторий. | ⛔ НЕ менять логику имён/строк — калибровка 30/30 с эталоном. |
| [enrich.py](enrich.py) | **Канонический** enrichment: `[путь][class Родитель]` + сигнатура + docstring + `--- код ---` + код. Единый источник правды (`build_enriched`, `build_enriched_map`) для индексатора и экспериментов. | ⚠️ менять — только синхронно переиндексировав (база станет несовместима с метрикой). |
| [enriched_chunks.py](enriched_chunks.py) | Тонкая обёртка-shim над `enrich.py`: re-export `build_enriched_map` (чтобы исторические импорты экспериментов не ломались) + CLI-дамп `cache/enriched_texts.json`. | можно дорабатывать |
| [index.py](index.py) | `python index.py <папка>` → строит коллекцию `codelens` в ChromaDB (`chroma_db/`). Эмбеддит ровно enriched-текст из `enrich.py`. Идемпотентен (пересоздаёт коллекцию с нуля). | можно дорабатывать |
| [search.py](search.py) | Боевое ядро: `search(query, top_k=5, use_hyde=True)`. HyDE temp=0 + mix (α=0.5), graceful fallback без Ollama, кэш генераций, разбивка latency, `NEGATIVE_THRESHOLD=60`. Есть CLI. | можно дорабатывать |
| [app.py](app.py) | Streamlit UI: вкладка **🔎 Поиск** (`st.code` подсветка, relevance %, latency, баннер «не найдено») + вкладка **📊 Метрики** (живой P@5 на 15 или 71). Кэш модели/БД через `@st.cache_resource`. | можно дорабатывать |
| [predict.py](predict.py) | `python predict.py` → пишет официальный `results.json` (формат сдачи) финальным ядром `search.py` на 15 вопросах. Падает, а не пишет выдачу без HyDE. **Для сдачи использовать его, а не `baseline.py`.** | можно дорабатывать |
| [sslfix.py](sslfix.py) | Чинит битый `SSL_CERT_FILE`/`REQUESTS_CA_BUNDLE`/… (импортируется первым в `index.py`/`search.py`/`exp_hyde_temp0.py`). См. §5. | не трогать |
| [score.py](score.py) | Скорер организаторов (P@5, `path`/`name` точно, `start_line` ±2). `score_question` переиспользуется и продуктом, и экспериментами. | ⛔ не модифицируем (метрика жюри). |

### 2.2 Эксперименты (исследовательский слой, для отчёта; в проде не используются)

| Файл | Что делает |
|---|---|
| [baseline.py](baseline.py) | Стартовая точка: MiniLM, СЫРОЙ код, косинус через numpy → пишет `results.json` → `score.py`. P@5=0.700 (офиц.15). ⚠️ **именно он перезаписывает верхнеуровневый `results.json`** — см. §6. |
| [eval_runner.py](eval_runner.py) | Универсальный harness: 4 модели × {raw, enriched} → `results/ablation_models.csv` + срезы. Префиксы query/passage для e5 и gte-Qwen2. Кэш эмбеддингов на конфиг. |
| [ablation_full.py](ablation_full.py) | Полная цепочка на 71 вопросе (15 офиц. + 56 расш.) → `results/ablation_full.csv` + парный бутстрап значимости. Собирает `eval/eval_combined.json`. |
| [hyde.py](hyde.py) | HyDE-генерация через Ollama (`qwen2.5-coder:7b`): системный промт + `ollama_generate` (temp=0 по умолчанию). Прод-`search.py` импортирует промт и вызов отсюда. Ablation base/pure/mix + α-свип → `results/ablation_hyde.csv`. |
| [hyde_multi.py](hyde_multi.py) | Multi-HyDE (×3 сэмпла, temp=0.7) → `results/ablation_hyde_multi.csv`. **Отклонён** (нет прироста), оставлен как evidence. |
| [rerank.py](rerank.py) | Cross-encoder `bge-reranker-v2-m3` поверх top-20 → `results/ablation_rerank.csv`. **Отклонён** (нейтрален на 71), оставлен как evidence. |
| [hybrid.py](hybrid.py) | BM25 (snake/camel-токенизация) + dense через RRF → `results/ablation_hybrid.csv`. **Отклонён** (оверфит на 15-q), оставлен как evidence. |
| [gen_eval.py](gen_eval.py) | Генератор расширенного eval: Ollama формулирует вопросы по чанкам gymhero (отдельные промты ru/en и для функций/DTO), детекторы утечки имени и копирования few-shot → `eval/eval_extended.json`. |
| [exp_hyde_temp0.py](exp_hyde_temp0.py) | Эксперимент детерминизма: temp=0.2-frozen vs live, temp=0 run1 vs run2 → `results/exp_hyde_temp0.json`. Показал, что 0.822 — это удачная выборка на 15-q, а temp=0 даёт 0.800/0.831 и метрически воспроизводим. |
| [analysis.ipynb](analysis.ipynb) | Графики для отчёта (8 ячеек). Читает `results/*.csv`, строит 3 PNG: `fig_model_bakeoff`, `fig_p5_by_config`, `fig_ru_vs_en`. |
| [check.ipynb](check.ipynb) | Sanity-check окружения: версии torch/CUDA/sentence-transformers/chromadb/faiss (подтверждает GPU = RTX 4080 Laptop). |

### 2.3 Данные и артефакты

| Путь | Что внутри |
|---|---|
| [gymhero/](gymhero/) | Индексируемый корпус: **47 `.py` → 155 чанков**. FastAPI/SQLAlchemy-приложение (распаковано из `codebase_python.zip`). |
| [codebase_python.zip](codebase_python.zip) | Исходный архив корпуса от организаторов (53 КБ, закоммичен). |
| [eval_questions.json](eval_questions.json) | Официальный eval — **15 вопросов**. ⛔ Ground truth, не перезаписывать. |
| [eval/eval_extended.json](eval/eval_extended.json) | Наш расширенный eval — **56 вопросов** (28 ru / 28 en; easy 18 / medium 28 / hard 10). |
| [eval/eval_combined.json](eval/eval_combined.json) | Объединённый набор — **71 вопрос** (15 + 56), с полем `subset`. Авто-генерится `ablation_full.py`; читается `app.py` (срез «расширенный»). |
| [sample_queries.txt](sample_queries.txt) | **20** демо-запросов, включая 3 «негатива» (OAuth2 / WebSocket / rate limiting; запрос про кеширование — фактически 4-й негатив). |
| [results.json](results.json) | Верхнеуровневый файл в формате сдачи (15 предсказаний) **финальной конфигурации** (bge-m3 + enriched + HyDE-mix, temp=0), **Total P@5 = 0.800**. Пересобирается `python predict.py`. Помни: `baseline.py` перезапишет его выдачей baseline — после прогона baseline всегда пересобирай `predict.py`. |
| [results/](results/) | 6 ablation-CSV + `exp_hyde_temp0.json` + 3 PNG-графика + 16 `preds_*.json` (предсказания всех конфигураций; финальный — `preds_bge-m3_enriched_hyde_mix.json`). |
| [cache/](cache/) | Закоммичен (~6.5 МБ): эмбеддинги чанков/вопросов по моделям (`.pkl`), enriched-тексты, HyDE-генерации (одиночные/мульти/query-cache), скоры реранкера. Делает ablation воспроизводимым без GPU. |
| `chroma_db/` | Persistent-база ChromaDB (в `.gitignore`, строится `index.py`, ~7 МБ на диске). |

### 2.4 Документация

| Файл | Для кого |
|---|---|
| [README.md](README.md) | Жюри + пользователь: что это, запуск двумя командами, примеры, обоснование чанкинга, метрики, таблица порога «не найдено». |
| [REPORT.md](REPORT.md) | Жюри (исследовательский дифференциатор): полная ablation, бутстрап-CI, кросс-язык, ограничения. |
| [DATASET.md](DATASET.md) | Описание датасета и формата chunk_id от организаторов (их исходный README, сохранён переименованием). |
| [CLAUDE.md](CLAUDE.md) | Технический контекст для AI-агента (Claude Code). |
| [HANDOFF.md](HANDOFF.md) | Этот файл — карта для команды. |

### 2.5 Что закоммичено, а что нет (состояние git по факту)

- **Рабочее дерево чистое.** Всё перечисленное выше — в git, кроме `chroma_db/`.
- **`.gitignore` — всего 3 строки:** `chroma_db/`, `__pycache__/`, `*.pyc`.
- **Закоммичено намеренно:** весь исходный код, корпус `gymhero/`, `codebase_python.zip`,
  eval-файлы, `results/` (CSV+PNG+preds), `cache/` (детерминированные артефакты — поэтому
  ablation повторяется без пересчёта на GPU), `.vscode/settings.json`, документация.
- **`__pycache__` убран из индекса git** (`git rm -r --cached __pycache__`, 10 устаревших
  `.pyc`, включая `cpython-314`): изменение застейджено как удаление, осталось закоммитить.
  На диск файлы не тронуты, git их больше не отслеживает (папка под `.gitignore`).
- **`chroma_db/`** не в git; на диске накопилось несколько HNSW-сегментов от пересборок
  (Chroma не всегда чистит старые) — безвредно, можно удалить папку и пересобрать `index.py`.

---

## 3. Как запустить (быстрый старт для нового участника)

```bash
conda activate codelens                       # окружение уже создано (Python 3.12)
pip install -r requirements.txt               # если с нуля
python index.py gymhero                        # 47 файлов → 155 чанков
streamlit run app.py                           # UI на http://localhost:8501
```

- HyDE требует Ollama: `ollama pull qwen2.5-coder:7b` (без неё — graceful fallback без HyDE,
  демо не падает).
- Модели качаются при первом запуске в `~/.cache/huggingface/` (bge-m3 ≈ 2.3 ГБ) — прогреть заранее.
- Индексация 155 чанков — десятки секунд на GPU (плюс разовая загрузка модели в первый раз).
- **Запускай скрипты интерпретатором env `codelens` явно** — голый `python` в этой системе
  резолвится в msys2 без ML-зависимостей (см. §5).

Окружение: Miniconda3, env `codelens`, torch cu128 (CUDA 13.3 драйвер), основная машина —
ноут RTX 4080 mobile 12 ГБ.

---

## 4. Ключевые решения и почему так (чтобы команда не переоткрывала)

- **Чанк = функция/класс/метод.** Минимальная семантическая единица, AST-детерминизм,
  совпадает с гранулярностью вопроса. Калибровка 30/30 с эталоном — формат chunk_id точный.
- **Эмбеддим обогащённый текст, не голый код.** Enrichment — единственный статистически
  значимый прирост (CI [+0.012, +0.099], 6/0). Это фундамент, не трогать без переиндексации.
- **bge-m3 как эмбеддер:** лучший на bake-off (enriched 0.767 на 15-q), мультиязычный, код-aware.
  gte-Qwen2 провалился (0.278/0.344 — не код-tuned). e5-large — близкая альтернатива (0.733,
  силён на ru). MiniLM (baseline) — 0.700.
- **HyDE-mix закрывает «RU-вопрос ↔ EN-код»:** LLM генерит гипотетический код с английскими
  именами → мост между языком вопроса и языком кода. Чинит слабые срезы (hard, en). Принят как прод.
- **temp=0 для воспроизводимости:** run1≡run2 по метрике, без замороженных артефактов.
  `cache/hyde_query_cache.json` — обычный кэш ускорения (пишется из живых temp=0 генераций), не frozen-сид.
- **Негативный порог 60%** откалиброван по распределению top-1 relevance (негативы ≤58.4,
  истинный пол позитивов 62.8 — чистый зазор). Влияет только на UI-баннер, P@5 не трогает.
- **Отклонённые техники — это фича, не баг.** rerank / hybrid / multi-HyDE проверены и отклонены
  по данным на 71 вопросе — материал для отчёта. Не возвращать без новых данных (особенно гибрид —
  он оверфит на 15-q).

---

## 5. Грабли (известные, задокументированы)

1. **`SSL_CERT_FILE` в Git Bash указывает на несуществующий cacert** → падает загрузка моделей HF
   (`FileNotFoundError` ещё до чтения локального кэша). Чинит [sslfix.py](sslfix.py) (импортируется
   первым). Глобально: `export SSL_CERT_FILE="$(python -c 'import certifi; print(certifi.where())')"`.
2. **MSYS2-python перехватывает shell.** Голый `python` может резолвиться в `C:\msys64\…` без
   ML-зависимостей. Запускать явно интерпретатором env `codelens`
   (`…/miniconda3/envs/codelens/python.exe`) или активировать env.
3. **gte-Qwen2 + transformers 5.x:** `trust_remote_code=True` падает (`'Qwen2Config' has no
   attribute 'rope_theta'` — remote-код предшествует RoPE-рефактору v5). Решение — нативный
   `Qwen2Model` (`trust_remote_code=False`). `transformers==5.11.0` запинен не случайно.
4. **0.822 (frozen) vs 0.800/0.831 (прод).** Заголовочные 0.822 на официальных 15 — это удачная
   выборка на frozen temp=0.2 генерациях. Живая генерация при любой температуре регрессирует к
   **0.800 (офиц.15)**, но на 71 даёт **0.831**. Прод = live temp=0; отчёт цитирует 0.800/0.831,
   не 0.822. (Старые temp=0.2-числа сохранены в `cache/`/ablation-CSV как историческая база A/B.)
5. **Детерминизм не побайтовый.** 2/71 HyDE-генерации (`q_13`, `g_08`) расходятся в формулировках,
   но top-5 и P@5 идентичны между прогонами. Единственный строго нулевой по вариативности вариант —
   замороженный кэш; мы выбрали воспроизводимость по метрике, а не по байтам.

---

## 6. Что осталось сделать (backlog для команды)

> Две находки по факту репозитория добавлены первым пунктом — их нельзя было знать из черновика.

**Приоритет 0 — гигиена сдачи** — ✅ закрыто 2026-06-13

- [x] **`results.json` пересобран финальной конфигурацией** через `python predict.py`
  (bge-m3 + enriched + HyDE-mix, temp=0; все 15 — с HyDE из кэша, без Ollama). Проверено
  `score.py`: **Total P@5 = 0.800** (easy 0.900 / medium 0.833 / hard 0.625; ru 0.792 / en 0.810).
- [x] **`__pycache__` убран из индекса git** (`git rm -r --cached __pycache__`) — осталось
  закоммитить вместе с новым `predict.py` и `HANDOFF.md`. См. §2.5.

**Приоритет 1 — требование ТЗ (формальный критерий)**

- [x] **База ≥80 файлов.** Сейчас `gymhero` = 47, индексируется только он. Добавить 1-2 открытых
  Python-репо (MIT/Apache) в индекс. ⚠️ Официальный eval считается только по gymhero — доп. репо
  не должны менять chunk_id gymhero и ломать его индексацию. Проверить, что P@5 на 15 официальных не упал.

**Приоритет 2 — бонусы (плюс к оценке)**

- [x] **LLM-чат** (дёшево, Ollama уже есть): найденные фрагменты + вопрос → `qwen2.5-coder` →
  связный ответ. Сейчас в `app.py` только 2 вкладки (Поиск, Метрики) — чата нет. Добавить третью вкладку.
- [x] **Docker Compose:** `docker compose up`. Корневого Dockerfile/compose нет (тот, что внутри
  `gymhero/`, — часть корпуса, не наш). Не ломать запуск двумя командами (оставить оба пути).
- [x] **Второй язык (JS/Java)** через tree-sitter — если останется время.

**Приоритет 3 — усиление ретрива (этим занимается напарник, параллельно)**

- [x] **Поднять HyDE до статзначимости:** расширить eval до ~170 вопросов (тем же `gen_eval.py`)
  ИЛИ другие рычаги. Текущий потолок — HyDE-mix, прирост направленно реален (CI [−0.016, +0.101]),
  но n=71 мало.
- [ ] Можно пробовать: код-tuned reranker (jina-reranker-v2), API-эмбеддеры (Cohere embed-v4,
  Voyage) для сравнения в отчёте. Любой новый рычаг мерить на 71, не на 15 (иначе оверфит).
- [x] Уточнить hard-вопросы, указывающие на класс вместо метода (напр. `g_09` → `UserCRUDRepository`:
  добавить метод как также-корректный эталон).

**Приоритет 4 — защита (10 мин: 7 демо + 3 Q&A)**

- [x] Демо-скрипт: запуск `index.py` вживую → живой поиск RU+EN → страница метрик → негативный
  запрос → (если будет) LLM-чат.
- [ ] Заготовить ответы на Q&A: чанкинг (главный вопрос!), выбор модели/БД, кросс-язык, масштаб.
- [ ] Репетиция тайминга. Заморозить репо (tag), проверить, что ссылка открывается без логина.

---

## 7. Принципы работы (как мы вели проект — стоит продолжить)

- **Каждое изменение метрики меряем на 71 вопросе, до/после, на `score.py`.** Не верим приростам
  на 15 вопросах (1 вопрос = 0.067, шум). Именно так поймали оверфит гибрида и «ложную» отбраковку реранка.
- **Отрицательный результат — это результат.** Если техника не помогла — документируем с числами
  и CI, это идёт в отчёт и усиливает защиту.
- **Воспроизводимость прежде красоты.** Кэшируем (cache/ в git), пинуем версии (requirements.txt),
  seed в бутстрапе. Прод не зависит от платного API-ключа.
- **Честность в числах.** Никаких подогнанных под демо порогов, никаких замороженных артефактов
  в проде, расхождения проговариваем явно (0.822 → 0.800/0.831).
- **Коммитим часто, осмысленными сообщениями** (история коммитов — часть оценки за чистоту репо).
