{
  "q_01": "import uuid\nfrom datetime import timedelta\n\nclass AccessToken:\n    def __init__(self, user_id):\n        self.token = str(uuid.uuid4())\n        self.user_id = user_id\n        self.expiry_time = timedelta(hours=1)\n\n    def is_valid(self):\n        return self.expiry_time > timedelta(0)",
  "q_02": "import jwt\n\ndef verify_jwt_token(token, secret_key):\n    \"\"\"\n    Verifies a JWT token and returns the decoded payload if valid.\n    \n    :param token: The JWT token to be verified.\n    :param secret_key: The secret key used for decoding the token.\n    :return: Decoded payload if the token is valid, otherwise raises an exception.\n    \"\"\"\n    try:\n        return jwt.decode(token, secret_key, algorithms=[\"HS256\"])\n    except jwt.ExpiredSignatureError:\n        raise ValueError(\"Token has expired\")\n    except jwt.InvalidTokenError:\n        raise ValueError(\"Invalid token\")",
  "q_03": "def is_superuser(user):\n    \"\"\"Check if the user is a superuser.\"\"\"\n    return user.is_superuser",
  "q_04": "def create_account(email, password):\n    \"\"\"\n    Create a new user account with the given email and password.\n    Returns a success message or an error if the email is already taken.\n    \"\"\"\n    if User.objects.filter(email=email).exists():\n        return \"Email already taken\"\n    else:\n        User.objects.create_user(email=email, password=password)\n        return \"Account created successfully\"",
  "q_05": "import bcrypt\n\nclass UserAuth:\n    def __init__(self, user_data):\n        self.user_data = user_data\n\n    def check_password(self, password_attempt):\n        \"\"\"\n        Check if the provided password matches the stored hash.\n        \"\"\"\n        return bcrypt.checkpw(password_attempt.encode('utf-8'), self.user_data['password_hash'].encode('utf-8'))",
  "q_06": "from sqlalchemy.orm import Session\n\nclass DatabaseManager:\n    def __init__(self, engine):\n        self.engine = engine\n\n    def get_session(self) -> Session:\n        \"\"\"Create and return a new database session.\"\"\"\n        from sqlalchemy import create_engine\n        engine = create_engine(self.engine)\n        return Session(bind=engine)\n\n# Usage in FastAPI endpoint\nfrom fastapi import Depends\n\ndef get_db():\n    db_manager = DatabaseManager(\"sqlite:///example.db\")\n    try:\n        yield db_manager.get_session()\n    finally:\n        db_manager.session.close()\n\n@app.post(\"/items/\")\nasync def create_item(item: Item, db: Session = Depends(get_db)):\n    # Use the database session here\n    pass",
  "q_07": "def create_db_connection_string(settings):\n    \"\"\"\n    Creates a connection string for PostgreSQL from application settings.\n    \n    :param settings: Dictionary containing database configuration settings.\n    :return: A string representing the PostgreSQL connection URL.\n    \"\"\"\n    return f\"postgresql://{settings['user']}:{settings['password']}@{settings['host']}:{settings['port']}/{settings['database']}\"",
  "q_08": "import os\n\nclass ConfigLoader:\n    \"\"\"Load configuration based on the runtime environment.\"\"\"\n    \n    def __init__(self):\n        self.config = {}\n    \n    def load_config(self, env):\n        \"\"\"Load configuration for the given environment.\"\"\"\n        config_file = f\"config.{env}.yaml\"\n        if not os.path.exists(config_file):\n            raise FileNotFoundError(f\"Configuration file {config_file} not found.\")\n        \n        with open(config_file, 'r') as file:\n            self.config = yaml.safe_load(file)\n    \n    def get_config(self, key):\n        \"\"\"Get a configuration value by key.\"\"\"\n        return self.config.get(key, None)\n\nThis class `ConfigLoader` loads configuration from YAML files based on the runtime environment. It checks for the existence of a configuration file named `config.{env}.yaml`, where `{env}` is the current environment (e.g., 'development', 'production'). If the file exists, it reads and parses the YAML content into a dictionary stored in `self.config`. The `get_config` method allows retrieving values from this dictionary.",
  "q_09": "class Repository:\n    def create_record(self, owner_id, data):\n        \"\"\"\n        Create a new record associated with the given owner.\n        \"\"\"\n        # Simulate database operation\n        record = {\n            'id': self.generate_id(),\n            'owner_id': owner_id,\n            **data\n        }\n        self.save_to_database(record)\n        return record\n\n    def generate_id(self):\n        \"\"\"\n        Generate a unique ID for a new record.\n        \"\"\"\n        import uuid\n        return str(uuid.uuid4())\n\n    def save_to_database(self, record):\n        \"\"\"\n        Save the record to the database.\n        \"\"\"\n        # Simulate saving to a database\n        print(f\"Saving record: {record}\")\n\nThis class `Repository` provides a method `create_record` that creates a new record and associates it with an owner. It includes methods for generating a unique ID and simulating saving the record to a database.",
  "q_10": "def handle_invalid_credentials():\n    \"\"\"\n    Returns an error response for invalid credentials with a 401 Unauthorized status code.\n    \"\"\"\n    return {\n        \"error\": \"Invalid credentials\",\n        \"status_code\": 401\n    }",
  "q_11": "def is_training_plan_unique(plan):\n    \"\"\"\n    Check if the given training plan is unique in the database.\n    \n    Args:\n        plan (dict): The training plan to check.\n        \n    Returns:\n        bool: True if the plan is unique, False otherwise.\n    \"\"\"\n    # Assuming a hypothetical function `get_existing_plans()` that fetches all existing plans from the database\n    existing_plans = get_existing_plans()\n    \n    for existing_plan in existing_plans:\n        if existing_plan['name'] == plan['name'] and existing_plan['intensity'] == plan['intensity']:\n            return False\n    \n    return True",
  "q_12": "class TrainingPlan:\n    def __init__(self):\n        self.units = set()\n\n    def add_unit(self, unit):\n        if unit in self.units:\n            raise ValueError(\"Unit already exists in the plan\")\n        self.units.add(unit)\n\nThis class `TrainingPlan` uses a set to store training units. The `add_unit` method checks if the unit is already in the set before adding it, raising a `ValueError` if it is.",
  "q_13": "class ExerciseSchema:\n    def __init__(self, name: str, description: str, duration_minutes: int):\n        self.name = name  # Required, string\n        self.description = description  # Required, string\n        self.duration_minutes = duration_minutes  # Required, integer, must be greater than 0\n\n    @property\n    def is_valid(self) -> bool:\n        return (\n            isinstance(self.name, str)\n            and isinstance(self.description, str)\n            and isinstance(self.duration_minutes, int)\n            and self.duration_minutes > 0\n        )",
  "q_14": "class TrainingPlanManager:\n    def __init__(self, user):\n        self.user = user\n\n    def can_delete(self, plan):\n        \"\"\"Check if the current user is the owner of the plan or a superuser.\"\"\"\n        return plan.owner == self.user or self.user.is_superuser\n\nThis class provides a method `can_delete` that checks whether the current user (self.user) is either the owner of the training plan (plan.owner) or has superuser privileges.",
  "q_15": "class DatabasePaginator:\n    \"\"\"\n    A class to handle pagination of database queries.\n    \"\"\"\n\n    def __init__(self, page_size=10, page_number=1):\n        self.page_size = page_size\n        self.page_number = page_number\n\n    def get_paginated_data(self, query):\n        \"\"\"\n        Returns paginated data based on the current page and page size.\n        \"\"\"\n        offset = (self.page_number - 1) * self.page_size\n        return query.limit(self.page_size).offset(offset)",
  "g_01": "class ExerciseData:\n    \"\"\"Represents the structure of training exercise data.\"\"\"\n    \n    def __init__(self, name: str, description: str, muscles_worked: list):\n        self.name = name\n        self.description = description\n        self.muscles_worked = muscles_worked\n\n    def to_dict(self) -> dict:\n        return {\n            \"name\": self.name,\n            \"description\": self.description,\n            \"muscles_worked\": self.muscles_worked\n        }",
  "g_02": "class BodyPartFetcher:\n    def __init__(self, api_client):\n        self.api_client = api_client\n\n    def fetch_body_parts(self, page_number=1, page_size=20):\n        \"\"\"\n        Fetches body parts with pagination.\n\n        :param page_number: The current page number to fetch.\n        :param page_size: The number of items per page.\n        :return: A list of body parts.\n        \"\"\"\n        response = self.api_client.get('/body_parts', params={'page': page_number, 'size': page_size})\n        return response.json()['body_parts']",
  "g_03": "class DatabaseConnection:\n    \"\"\"Base class for managing database connections.\"\"\"\n    def connect(self):\n        raise NotImplementedError\n\n    def disconnect(self):\n        raise NotImplementedError\n\n\nclass SQLDatabase(DatabaseConnection):\n    \"\"\"Class for interacting with SQL databases.\"\"\"\n    def connect(self):\n        # Implementation to connect to an SQL database\n        pass\n\n    def disconnect(self):\n        # Implementation to disconnect from an SQL database\n        pass\n\n\nclass NoSQLDatabase(DatabaseConnection):\n    \"\"\"Class for interacting with NoSQL databases.\"\"\"\n    def connect(self):\n        # Implementation to connect to a NoSQL database\n        pass\n\n    def disconnect(self):\n        # Implementation to disconnect from a NoSQL database\n        pass",
  "g_04": "class TrainingUnitSchema:\n    \"\"\"Represents the schema for a training unit.\"\"\"\n    \n    def __init__(self, id, title, description, duration):\n        self.id = id\n        self.title = title\n        self.description = description\n        self.duration = duration",
  "g_05": "class DatabaseConnection:\n    \"\"\"Context manager for managing database connections.\"\"\"\n    \n    def __init__(self, db_url):\n        self.db_url = db_url\n        self.connection = None\n\n    def __enter__(self):\n        import sqlite3\n        self.connection = sqlite3.connect(self.db_url)\n        return self.connection\n\n    def __exit__(self, exc_type, exc_val, exc_tb):\n        if self.connection:\n            self.connection.close()",
  "g_06": "class UserRegistrationSchema:\n    \"\"\"Schema for validating user registration data.\"\"\"\n    \n    def validate(self, data):\n        # Implementation of validation logic here\n        pass",
  "g_07": "import psycopg2\n\nclass DatabaseConnection:\n    \"\"\"Class to manage database connection.\"\"\"\n    \n    def __init__(self, dbname, user, password, host='localhost', port=5432):\n        self.connection = None\n        self.dbname = dbname\n        self.user = user\n        self.password = password\n        self.host = host\n        self.port = port\n    \n    def connect(self):\n        \"\"\"Establish a connection to the database.\"\"\"\n        try:\n            self.connection = psycopg2.connect(\n                dbname=self.dbname,\n                user=self.user,\n                password=self.password,\n                host=self.host,\n                port=self.port\n            )\n            print(\"Database connection established.\")\n        except Exception as e:\n            print(f\"Error connecting to database: {e}\")\n    \n    def close(self):\n        \"\"\"Close the database connection.\"\"\"\n        if self.connection:\n            self.connection.close()\n            print(\"Database connection closed.\")",
  "g_08": "class ExerciseFetcher:\n    def fetch_exercises(self, user_id):\n        \"\"\"Fetches exercises for a specific user.\"\"\"\n        # Simulate database query to get exercises for the user\n        exercises = self._get_user_exercises_from_db(user_id)\n        return exercises\n\n    def _get_user_exercises_from_db(self, user_id):\n        \"\"\"Simulated method to fetch exercises from the database.\"\"\"\n        # Placeholder for actual database logic\n        return [\n            {\"id\": 1, \"name\": \"Push-ups\", \"user_id\": user_id},\n            {\"id\": 2, \"name\": \"Squats\", \"user_id\": user_id}\n        ]",
  "g_09": "class User:\n    def deactivate(self):\n        \"\"\"Deactivate the user.\"\"\"\n        self.is_active = False",
  "g_10": "class TrainingUnitPagination:\n    def __init__(self, page_size=10):\n        self.page_size = page_size\n\n    def get_page(self, page_number, total_units):\n        start_index = (page_number - 1) * self.page_size\n        end_index = start_index + self.page_size\n        return total_units[start_index:end_index]",
  "g_11": "class WorkoutPlan:\n    def remove_exercise(self, exercise_id):\n        \"\"\"Remove an exercise from the workout plan.\"\"\"\n        self.exercises = [e for e in self.exercises if e.id != exercise_id]",
  "g_12": "class ExerciseTypeService:\n    def get_all_exercise_types(self, page_number, page_size):\n        \"\"\"\n        Fetches all exercise types with pagination.\n\n        Args:\n            page_number (int): The current page number.\n            page_size (int): The number of items per page.\n\n        Returns:\n            list: A list of exercise type objects for the specified page.\n        \"\"\"\n        offset = (page_number - 1) * page_size\n        return ExerciseType.objects.all()[offset:offset + page_size]",
  "g_13": "def find_part_body_by_name(part_name):\n    \"\"\"\n    Finds the body of a part by its name.\n    \"\"\"\n    # Implementation goes here\n    pass",
  "g_14": "class LevelService:\n    def get_level_by_id(self, level_id):\n        \"\"\"\n        Retrieve a specific level by its ID.\n        \n        Args:\n            level_id (int): The ID of the level to retrieve.\n            \n        Returns:\n            dict: The level data if found, otherwise None.\n        \"\"\"\n        # Simulate fetching from a database\n        levels = {\n            1: {'id': 1, 'name': 'Easy', 'description': 'Beginner level'},\n            2: {'id': 2, 'name': 'Medium', 'description': 'Intermediate level'},\n            3: {'id': 3, 'name': 'Hard', 'description': 'Advanced level'}\n        }\n        return levels.get(level_id)",
  "g_15": "class DevelopmentConfig:\n    \"\"\"Configuration for development environment.\"\"\"\n    DEBUG = True\n    DATABASE_URI = 'sqlite:///dev.db'\n    SECRET_KEY = 'development_secret_key'",
  "g_16": "class LevelFetcher:\n    def __init__(self, api_client):\n        self.api_client = api_client\n\n    def fetch_all_levels(self, page_size=10, page_number=1):\n        \"\"\"\n        Fetches all levels with pagination.\n        \"\"\"\n        while True:\n            response = self.api_client.get_levels(page_size=page_size, page_number=page_number)\n            if not response.levels:\n                break\n            for level in response.levels:\n                yield level\n            page_number += 1",
  "g_17": "class ExercisePagination:\n    def __init__(self, page_size=10):\n        self.page_size = page_size\n\n    def paginate_exercises(self, exercises):\n        \"\"\"\n        Paginate a list of exercises.\n        \"\"\"\n        return [exercises[i:i + self.page_size] for i in range(0, len(exercises), self.page_size)]",
  "g_18": "class LevelRepository:\n    def get_level_by_name(self, level_name):\n        \"\"\"\n        Fetches a level by its name from the database.\n        \"\"\"\n        # Assuming db is an instance of a database connection\n        query = \"SELECT * FROM levels WHERE name = %s\"\n        result = self.db.execute(query, (level_name,))\n        return result.fetchone()",
  "g_19": "def get_all_users(page, page_size):\n    \"\"\"\n    Retrieve all users with pagination.\n\n    :param page: Current page number (1-based)\n    :param page_size: Number of items per page\n    :return: List of users for the specified page\n    \"\"\"\n    offset = (page - 1) * page_size\n    return User.query.limit(page_size).offset(offset).all()",
  "g_20": "class TrainingUnit:\n    def __init__(self):\n        self.exercises = []\n\n    def remove_exercise(self, exercise_id):\n        \"\"\"Remove an exercise by its ID.\"\"\"\n        self.exercises = [exercise for exercise in self.exercises if exercise['id'] != exercise_id]",
  "g_21": "def update_exercise_type(exercise_id, new_type):\n    \"\"\"\n    Updates the type of an exercise with the given ID.\n    \"\"\"\n    # Implementation goes here",
  "g_22": "class TestSettingsConfig:\n    \"\"\"Configure settings for testing.\"\"\"\n    \n    def __init__(self):\n        self.DEBUG = False\n        self.DATABASES = {\n            'default': {\n                'ENGINE': 'django.db.backends.sqlite3',\n                'NAME': ':memory:',\n            }\n        }\n        self.CACHES = {\n            'default': {\n                'BACKEND': 'django.core.cache.backends.locmem.LocMemCache',\n            }\n        }",
  "g_23": "class ExerciseService:\n    def get_exercises_for_workout(self, workout_id):\n        \"\"\"Retrieve all exercises for a specific workout.\"\"\"\n        # Implementation details here\n        pass",
  "g_24": "class ExerciseTypeManager:\n    def delete_exercise_type(self, exercise_type_id):\n        \"\"\"\n        Deletes an exercise type by its ID.\n        \"\"\"\n        # Assuming there's a database session and an ExerciseType model\n        session = get_db_session()\n        exercise_type = session.query(ExerciseType).filter_by(id=exercise_type_id).first()\n        if exercise_type:\n            session.delete(exercise_type)\n            session.commit()",
  "g_25": "def find_exercise_by_name(exercise_name):\n    \"\"\"\n    Searches for an exercise by its name.\n    \"\"\"\n    # Assuming exercises is a list of dictionaries with 'name' key\n    exercises = [\n        {'name': 'Push-ups', 'id': 1},\n        {'name': 'Squats', 'id': 2},\n        {'name': 'Bench Press', 'id': 3}\n    ]\n    \n    for exercise in exercises:\n        if exercise['name'].lower() == exercise_name.lower():\n            return exercise\n    return None",
  "g_26": "def is_exercise_in_training_unit(exercise_id, training_units):\n    \"\"\"\n    Check if an exercise belongs to any of the given training units.\n    \n    :param exercise_id: ID of the exercise to check\n    :param training_units: List of training units to check against\n    :return: True if the exercise is in any training unit, False otherwise\n    \"\"\"\n    return any(exercise_id in unit['exercises'] for unit in training_units)",
  "g_27": "def get_training_units_for_owner(owner_id, page=1, per_page=10):\n    \"\"\"\n    Retrieve all training units for a given owner with pagination.\n    \"\"\"\n    offset = (page - 1) * per_page\n    return TrainingUnit.objects.filter(owner_id=owner_id).order_by('created_at')[offset:offset + per_page]",
  "g_28": "class TrainingPlan:\n    \"\"\"Data structure for storing training plan information.\"\"\"\n    \n    def __init__(self, name, exercises):\n        self.name = name\n        self.exercises = exercises\n\n    def add_exercise(self, exercise):\n        self.exercises.append(exercise)\n\n    def get_exercises(self):\n        return self.exercises",
  "g_29": "class TrainingPlanAccessChecker:\n    \"\"\"\n    Checks if the user has access to a training plan.\n    \"\"\"\n    \n    def has_access(self, user_id: int, plan_id: int) -> bool:\n        # Logic to check if the user can access the training plan\n        pass",
  "g_30": "import logging\n\ndef setup_logger(name):\n    \"\"\"\n    Configure a logger with a specific name.\n\n    Args:\n        name (str): The name of the logger.\n\n    Returns:\n        logging.Logger: A configured logger instance.\n    \"\"\"\n    logger = logging.getLogger(name)\n    logger.setLevel(logging.DEBUG)\n\n    # Create console handler and set level to debug\n    ch = logging.StreamHandler()\n    ch.setLevel(logging.DEBUG)\n\n    # Create formatter\n    formatter = logging.Formatter('%(asctime)s - %(name)s - %(levelname)s - %(message)s')\n\n    # Add formatter to ch\n    ch.setFormatter(formatter)\n\n    # Add ch to logger\n    logger.addHandler(ch)\n\n    return logger",
  "g_31": "class ExerciseStructure:\n    \"\"\"Represents the structure of an exercise in the project.\"\"\"\n    \n    def __init__(self, title: str, description: str, difficulty: int):\n        self.title = title\n        self.description = description\n        self.difficulty = difficulty\n\n    def to_dict(self) -> dict:\n        return {\n            \"title\": self.title,\n            \"description\": self.description,\n            \"difficulty\": self.difficulty\n        }",
  "g_32": "class BodyPartSchema:\n    \"\"\"Schema for representing a body part.\"\"\"\n    \n    def __init__(self, name, description):\n        self.name = name\n        self.description = description\n\n    @classmethod\n    def from_dict(cls, data):\n        return cls(data['name'], data['description'])",
  "g_33": "class TrainingPlanService:\n    def get_all_training_plans(self, user_id):\n        \"\"\"Retrieve all training plans for a given user.\"\"\"\n        # Implementation details here\n        pass",
  "g_34": "class TrainingPlanService:\n    def get_all_training_plans(self, page_number, page_size):\n        \"\"\"\n        Retrieve all training plans with pagination.\n\n        :param page_number: The current page number.\n        :param page_size: The number of items per page.\n        :return: A list of training plans for the specified page.\n        \"\"\"\n        offset = (page_number - 1) * page_size\n        return TrainingPlan.objects.all()[offset:offset + page_size]",
  "g_35": "class JwtTokenStructure:\n    \"\"\"\n    Describes the structure of a JWT token.\n    \"\"\"\n    def __init__(self):\n        self.header = {\n            \"alg\": \"HS256\",\n            \"typ\": \"JWT\"\n        }\n        self.payload = {\n            \"sub\": None,\n            \"exp\": None\n        }\n        self.signature = None\n\n    def set_payload(self, sub: str, exp: int):\n        \"\"\"\n        Sets the payload of the JWT token.\n        :param sub: Subject of the token (usually a user ID)\n        :param exp: Expiration time in seconds since epoch\n        \"\"\"\n        self.payload[\"sub\"] = sub\n        self.payload[\"exp\"] = exp\n\n    def generate_signature(self, secret_key: str):\n        \"\"\"\n        Generates the signature for the JWT token.\n        :param secret_key: Secret key used to sign the token\n        \"\"\"\n        import jwt\n        encoded_jwt = jwt.encode(self.header | {\"payload\": self.payload}, secret_key, algorithm=\"HS256\")\n        self.signature = encoded_jwt.split('.')[2]",
  "g_36": "class TrainingUnit:\n    def add_existing_exercise(self, exercise_id):\n        \"\"\"\n        Adds an existing exercise to this training unit.\n        \"\"\"\n        self.exercises.append(exercise_id)",
  "g_37": "class LevelCreator:\n    def create_level(self, level_data):\n        \"\"\"\n        Creates a new level based on the provided data.\n        \n        Args:\n            level_data (dict): A dictionary containing level configuration.\n        \n        Returns:\n            bool: True if the level was created successfully, False otherwise.\n        \n        Raises:\n            ValueError: If the level data is invalid.\n        \"\"\"\n        try:\n            # Simulate level creation logic\n            if not level_data.get('name'):\n                raise ValueError(\"Level name is required\")\n            print(f\"Creating level: {level_data['name']}\")\n            return True\n        except Exception as e:\n            print(f\"Error creating level: {e}\")\n            return False",
  "g_38": "import bcrypt\n\nclass PasswordManager:\n    \"\"\"\n    Manages password hashing and verification.\n    \"\"\"\n\n    @staticmethod\n    def hash_password(password: str) -> bytes:\n        \"\"\"Hashes a plain text password.\"\"\"\n        return bcrypt.hashpw(password.encode('utf-8'), bcrypt.gensalt())\n\n    @staticmethod\n    def verify_password(stored_password: bytes, provided_password: str) -> bool:\n        \"\"\"Verifies if the provided password matches the stored hash.\"\"\"\n        return bcrypt.checkpw(provided_password.encode('utf-8'), stored_password)",
  "g_39": "class DatabaseManager:\n    def create_record(self, data):\n        \"\"\"Create a new record in the database.\"\"\"\n        # Implementation details here\n        pass",
  "g_40": "class UserTrainingPlanSchema:\n    \"\"\"Schema for defining a user's training plan.\"\"\"\n    \n    def __init__(self, user_id, exercises, duration):\n        self.user_id = user_id\n        self.exercises = exercises\n        self.duration = duration",
  "g_41": "class ExerciseManager:\n    def __init__(self, block_id):\n        self.block_id = block_id\n\n    def get_exercises(self):\n        \"\"\"Retrieve exercises associated with the specific training block.\"\"\"\n        # Simulate fetching exercises from a database or file\n        exercises = {\n            'block1': ['Exercise 1', 'Exercise 2'],\n            'block2': ['Exercise 3', 'Exercise 4']\n        }\n        return exercises.get(self.block_id, [])",
  "g_42": "class UserSchema:\n    \"\"\"Defines the schema for user data.\"\"\"\n    \n    def __init__(self):\n        self.id = None\n        self.username = None\n        self.email = None\n        self.password_hash = None\n        self.created_at = None\n        self.updated_at = None",
  "g_43": "class LocalTestSettings:\n    \"\"\"Configuration for local testing.\"\"\"\n    \n    DATABASE_URL = \"sqlite:///test.db\"\n    DEBUG = True\n    SECRET_KEY = \"dev_secret_key\"\n\nThis class `LocalTestSettings` encapsulates the configuration needed for local testing, such as database URL, debug mode, and secret key.",
  "g_44": "class TrainingPlanService:\n    def get_training_plan_by_id(self, plan_id, user):\n        \"\"\"\n        Retrieves a training plan by its ID, considering user privileges.\n        \"\"\"\n        # Check if the user has permission to access the plan\n        if not self.has_access(user, plan_id):\n            raise PermissionError(\"User does not have permission to access this training plan.\")\n        \n        # Fetch the training plan from the database\n        plan = TrainingPlan.objects.get(id=plan_id)\n        \n        return plan\n\n    def has_access(self, user, plan_id):\n        \"\"\"\n        Determines if the user has permission to access the specified training plan.\n        \"\"\"\n        # Implement logic to check user permissions\n        # For example, check if the user is an admin or the owner of the plan\n        return user.is_admin or user.id == TrainingPlan.objects.get(id=plan_id).owner_id",
  "g_45": "def find_user_by_id(user_id):\n    \"\"\"\n    Find a user by their unique identifier.\n    \"\"\"\n    # Assuming users is a dictionary where keys are user IDs and values are user objects\n    return users.get(user_id, None)",
  "g_46": "class ExerciseSchema:\n    \"\"\"Defines the schema for an exercise.\"\"\"\n    \n    def __init__(self, id, name, description, difficulty):\n        self.id = id\n        self.name = name\n        self.description = description\n        self.difficulty = difficulty",
  "g_47": "class AuthTokenGenerator:\n    \"\"\"\n    Generates access tokens for authenticated users.\n    \"\"\"\n\n    def generate_token(self, user_id: str) -> str:\n        # Simulate token generation logic\n        return f\"token_{user_id}\"\n\nThis class `AuthTokenGenerator` has a method `generate_token` that takes a `user_id` and returns a simulated access token. In a real-world application, this would involve more complex logic, such as using cryptographic libraries to create secure tokens.",
  "g_48": "class TrainingPlan:\n    def remove_training_unit(self, unit_id):\n        \"\"\"\n        Removes a training unit from the plan by its ID.\n        Raises ValueError if the unit is not found.\n        \"\"\"\n        try:\n            self.units.remove(unit_id)\n        except ValueError:\n            raise ValueError(f\"Training unit with ID {unit_id} not found in the plan.\")",
  "g_49": "def find_user_by_email(email):\n    \"\"\"\n    Find a user by their email address.\n    \"\"\"\n    # Implementation goes here\n    pass",
  "g_50": "class DatabaseUpdater:\n    def update_record(self, table_name, record_id, new_data):\n        \"\"\"\n        Updates a record in the specified table with new data.\n        \n        :param table_name: str - The name of the table to update.\n        :param record_id: int - The ID of the record to update.\n        :param new_data: dict - A dictionary containing the new data for the record.\n        \"\"\"\n        # Simulate database update\n        print(f\"Updating {table_name} with ID {record_id} with data {new_data}\")",
  "g_51": "class RecordManager:\n    \"\"\"\n    Manages records and provides methods to retrieve them by owner.\n    \"\"\"\n\n    def get_records_by_owner(self, owner_id):\n        \"\"\"\n        Retrieves all records owned by the given owner ID.\n\n        :param owner_id: The ID of the owner whose records are to be retrieved.\n        :return: A list of records owned by the specified owner.\n        \"\"\"\n        # Implementation details would go here\n        pass",
  "g_52": "class TrainingUnit:\n    def __init__(self):\n        self.exercises = []\n\n    def add_exercise(self, exercise):\n        \"\"\"Add an exercise to the training unit.\"\"\"\n        self.exercises.append(exercise)\n\n    def remove_exercise(self, exercise):\n        \"\"\"Remove an exercise from the training unit if it exists.\"\"\"\n        if exercise in self.exercises:\n            self.exercises.remove(exercise)",
  "g_53": "class AccessToken:\n    \"\"\"Represents an access token with its properties.\"\"\"\n    \n    def __init__(self, token: str, expires_at: datetime.datetime):\n        self.token = token\n        self.expires_at = expires_at\n\n    @property\n    def is_expired(self) -> bool:\n        return datetime.datetime.now() > self.expires_at",
  "g_54": "class ExerciseTypeSchema:\n    \"\"\"Schema for defining exercise types.\"\"\"\n    \n    def __init__(self, name, description):\n        self.name = name\n        self.description = description\n\n    def to_dict(self):\n        return {\n            'name': self.name,\n            'description': self.description\n        }",
  "g_55": "class TrainingPlanUpdateHandler:\n    def update_training_plan(self, user_id, plan_id, new_data):\n        \"\"\"\n        Updates the training plan for a given user if the user has the necessary permissions.\n        \"\"\"\n        if not self.has_permission(user_id, plan_id):\n            raise PermissionError(\"User does not have permission to update this training plan.\")\n        \n        # Logic to update the training plan\n        pass\n\n    def has_permission(self, user_id, plan_id):\n        \"\"\"\n        Checks if the user has permission to update the specified training plan.\n        \"\"\"\n        # Implementation of permission checking logic\n        return True  # Placeholder",
  "g_56": "class ExerciseTypeSchema:\n    \"\"\"Schema for defining exercise types.\"\"\"\n    \n    def __init__(self):\n        self.types = {\n            \"pushups\": {\"description\": \"Upper body strength training\", \"reps\": True},\n            \"squats\": {\"description\": \"Lower body strength training\", \"reps\": True},\n            \"planks\": {\"description\": \"Core stability exercise\", \"time\": True}\n        }\n    \n    def get_type(self, type_name):\n        \"\"\"Retrieve details about a specific exercise type.\"\"\"\n        return self.types.get(type_name)",
  "g_57": "def get_body_part_by_id(body_id):\n    \"\"\"\n    Retrieve one body part by its identifier through the API.\n    \"\"\"\n    response = requests.get(f\"https://api.example.com/body_parts/{body_id}\")\n    if response.status_code == 200:\n        return response.json()\n    else:\n        raise Exception(f\"Failed to retrieve body part: {response.status_code}\")",
  "g_58": "class BodyPartManager:\n    def __init__(self):\n        self.body_parts = {}\n\n    def add_body_part(self, name):\n        \"\"\"\n        Adds a new body part. Returns an error if the body part already exists.\n        \"\"\"\n        if name in self.body_parts:\n            return f\"Error: Body part '{name}' already exists.\"\n        else:\n            self.body_parts[name] = True\n            return \"Body part added successfully.\"\n\n# Example usage:\nmanager = BodyPartManager()\nprint(manager.add_body_part(\"Arm\"))  # Output: Body part added successfully.\nprint(manager.add_body_part(\"Arm\"))  # Output: Error: Body part 'Arm' already exists.",
  "g_59": "class BodyPart:\n    def __init__(self, identifier):\n        self.identifier = identifier\n\nclass Patient:\n    def __init__(self):\n        self.body_parts = []\n\n    def remove_body_part(self, body_part_id, authorized_person):\n        if authorized_person.is_authorized_to_remove():\n            for part in self.body_parts:\n                if part.identifier == body_part_id:\n                    self.body_parts.remove(part)\n                    return True\n        return False\n\nclass HealthcareWorker:\n    def __init__(self, is_authorized=False):\n        self.is_authorized = is_authorized\n\n    def is_authorized_to_remove(self):\n        return self.is_authorized",
  "g_60": "def update_body_part(body_part_id, new_data):\n    \"\"\"\n    Update an existing body part by its ID.\n    \n    :param body_part_id: int - The ID of the body part to update.\n    :param new_data: dict - A dictionary containing the new data for the body part.\n    :return: bool - True if the update was successful, False otherwise.\n    \"\"\"\n    # Simulate database update\n    existing_body_parts = {\n        1: {'name': 'Arm', 'type': 'Upper'},\n        2: {'name': 'Leg', 'type': 'Lower'}\n    }\n    \n    if body_part_id in existing_body_parts:\n        existing_body_parts[body_part_id].update(new_data)\n        return True\n    else:\n        return False",
  "g_61": "def get_exercise_by_id(exercise_id):\n    \"\"\"\n    Retrieve an exercise by its ID.\n    If the exercise is not found, raise a ValueError with a message indicating that the exercise does not exist.\n    \"\"\"\n    exercises = {\n        1: \"Exercise 1\",\n        2: \"Exercise 2\",\n        # ... other exercises\n    }\n    \n    if exercise_id in exercises:\n        return exercises[exercise_id]\n    else:\n        raise ValueError(f\"Exercise with ID {exercise_id} does not exist.\")",
  "g_62": "class ExerciseService:\n    def __init__(self, db):\n        self.db = db\n\n    def add_exercise(self, user_id, exercise_data):\n        \"\"\"\n        Adds a new exercise for a given user.\n        \n        :param user_id: ID of the user adding the exercise\n        :param exercise_data: Dictionary containing details about the exercise\n        :return: True if the exercise was added successfully, False otherwise\n        \"\"\"\n        # Check if the user is allowed to add exercises\n        if not self.is_user_allowed(user_id):\n            return False\n\n        # Add the exercise to the database\n        try:\n            self.db.insert('exercises', exercise_data)\n            return True\n        except Exception as e:\n            print(f\"Error adding exercise: {e}\")\n            return False\n\n    def is_user_allowed(self, user_id):\n        \"\"\"\n        Determines if a user is allowed to add exercises.\n        \n        :param user_id: ID of the user\n        :return: True if the user is allowed, False otherwise\n        \"\"\"\n        # Example logic: only users with role 'admin' can add exercises\n        return self.db.get_user_role(user_id) == 'admin'",
  "g_63": "def update_exercise(exercise_id, new_data):\n    \"\"\"\n    Update an existing exercise via the API.\n    \n    Returns the updated exercise data if successful, or None if the exercise does not exist.\n    \"\"\"\n    response = requests.put(f'https://api.example.com/exercises/{exercise_id}', json=new_data)\n    if response.status_code == 200:\n        return response.json()\n    elif response.status_code == 404:\n        return None",
  "g_64": "class ExerciseManager:\n    def __init__(self):\n        self.exercises = {}\n\n    def remove_exercise(self, exercise_id, user):\n        \"\"\"\n        Removes an exercise by ID if the user has permission.\n        \"\"\"\n        if exercise_id in self.exercises and self.has_permission(user):\n            del self.exercises[exercise_id]\n            return True\n        return False\n\n    def has_permission(self, user):\n        \"\"\"\n        Checks if the user has permission to remove exercises.\n        \"\"\"\n        # Implement permission logic here\n        return user.role == 'admin'",
  "g_65": "def get_exercise_type_by_id(exercise_id):\n    \"\"\"\n    Get the type of exercise by its identifier.\n    \"\"\"\n    # Assuming a dictionary mapping exercise IDs to types\n    exercise_types = {\n        1: 'strength',\n        2: 'cardio',\n        3: 'flexibility'\n    }\n    return exercise_types.get(exercise_id, 'unknown')",
  "g_66": "class ExerciseType:\n    def __init__(self, name, description):\n        self.name = name\n        self.description = description\n\nclass ExerciseTypeFinder:\n    def __init__(self, exercise_types):\n        self.exercise_types = {exercise.name: exercise for exercise in exercise_types}\n\n    def find_by_name(self, name):\n        \"\"\"Find an exercise type by its name.\"\"\"\n        return self.exercise_types.get(name)",
  "g_67": "class ExerciseType:\n    def __init__(self, name):\n        self.name = name\n\n    @classmethod\n    def add_type(cls, new_type):\n        if any(t.name == new_type.name for t in cls._types):\n            raise ValueError(f\"Exercise type with name '{new_type.name}' already exists.\")\n        cls._types.append(new_type)\n\n    _types = []\n\n# Example usage:\ntry:\n    ExerciseType.add_type(ExerciseType(\"Push-ups\"))\nexcept ValueError as e:\n    print(e)",
  "g_68": "from flask import Flask, request, jsonify\n\napp = Flask(__name__)\n\n# Simulated database of difficulty levels\ndifficulty_levels = {\n    1: \"Easy\",\n    2: \"Medium\",\n    3: \"Hard\"\n}\n\n@app.route('/difficulties/<int:id>', methods=['DELETE'])\ndef remove_difficulty(id):\n    \"\"\"\n    Removes a difficulty level by its ID.\n    \n    Only admins can call this endpoint.\n    \"\"\"\n    if 'admin' not in request.headers.get('Authorization', '').split():\n        return jsonify({\"error\": \"Unauthorized\"}), 403\n    \n    if id in difficulty_levels:\n        del difficulty_levels[id]\n        return jsonify({\"message\": f\"Difficulty level {id} removed successfully.\"}), 200\n    else:\n        return jsonify({\"error\": \"Difficulty level not found.\"}), 404\n\nif __name__ == '__main__':\n    app.run(debug=True)",
  "g_69": "def update_difficulty_by_id(task_id, new_difficulty):\n    \"\"\"\n    Update the difficulty level of a task by its ID.\n    \n    :param task_id: int - The unique identifier of the task.\n    :param new_difficulty: str - The new difficulty level ('easy', 'medium', 'hard').\n    :return: bool - True if the update was successful, False otherwise.\n    \"\"\"\n    # Simulate updating a database or in-memory data structure\n    tasks = {\n        1: {'id': 1, 'difficulty': 'easy'},\n        2: {'id': 2, 'difficulty': 'medium'},\n        3: {'id': 3, 'difficulty': 'hard'}\n    }\n    \n    if task_id in tasks:\n        tasks[task_id]['difficulty'] = new_difficulty\n        return True\n    else:\n        return False",
  "g_70": "def get_training_plan_by_name(plan_name):\n    \"\"\"\n    Look up a training plan by its name.\n\n    Args:\n        plan_name (str): The name of the training plan to look up.\n\n    Returns:\n        dict: The training plan dictionary if found, None otherwise.\n    \"\"\"\n    # Assuming training_plans is a dictionary where keys are plan names\n    return next((plan for plan in training_plans.values() if plan['name'] == plan_name), None)",
  "g_71": "def find_training_plans_by_name(superuser, plan_name):\n    \"\"\"\n    Searches for training plans by name among all owners.\n    \n    :param superuser: The superuser object with access to the database.\n    :param plan_name: The name of the training plan to search for.\n    :return: A list of training plans matching the name.\n    \"\"\"\n    return superuser.database.query('SELECT * FROM training_plans WHERE name LIKE ?', (f'%{plan_name}%',))",
  "g_72": "class TrainingPlan:\n    def __init__(self):\n        self.units = []\n\n    def add_unit(self, unit):\n        self.units.append(unit)\n\n    def remove_unit(self, unit_id):\n        \"\"\"Detach a training unit from the training plan by its ID.\"\"\"\n        self.units = [unit for unit in self.units if unit.id != unit_id]",
  "g_73": "def get_training_block(block_id):\n    \"\"\"\n    Retrieve the training block by its identifier.\n    \"\"\"\n    # Assuming a dictionary to simulate database access\n    training_blocks = {\n        'block1': {'title': 'Introduction', 'content': 'Welcome to the course'},\n        'block2': {'title': 'Module 1', 'content': 'Detailed explanation'}\n    }\n    return training_blocks.get(block_id, None)",
  "g_74": "def get_training_unit_by_name(name):\n    \"\"\"\n    Retrieve a training unit by its name.\n\n    :param name: The name of the training unit to retrieve.\n    :return: The training unit if found, otherwise None.\n    \"\"\"\n    # Assuming training_units is a dictionary where keys are names and values are training units\n    return training_units.get(name)",
  "g_75": "def find_training_blocks_by_name(name):\n    \"\"\"\n    Find training blocks by name.\n\n    :param name: str, the name of the training block to search for.\n    :return: list of TrainingBlock objects that match the given name.\n    \"\"\"\n    return [block for block in training_blocks if block.name == name]",
  "g_76": "from fastapi import APIRouter, HTTPException\n\nrouter = APIRouter()\n\n@router.post(\"/training_units/\")\nasync def create_training_unit(name: str):\n    \"\"\"\n    Create a new training unit.\n    \n    Raises:\n        HTTPException 409 if the training unit name already exists.\n    \"\"\"\n    # Simulate checking for existing name\n    if name in existing_training_units:\n        raise HTTPException(status_code=409, detail=\"Training unit with this name already exists.\")\n    \n    # Simulate creating new training unit\n    existing_training_units.add(name)\n    return {\"message\": \"Training unit created successfully.\"}",
  "g_77": "def update_training_block(block_id, new_data):\n    \"\"\"\n    Update the training block with the given ID and new data.\n    If the block does not exist, create a new one.\n    \"\"\"\n    try:\n        existing_block = TrainingBlock.objects.get(id=block_id)\n        existing_block.data = new_data\n        existing_block.save()\n    except TrainingBlock.DoesNotExist:\n        TrainingBlock.objects.create(id=block_id, data=new_data)",
  "g_78": "class TrainingUnitManager:\n    def __init__(self):\n        self.units = {}\n\n    def remove_training_unit(self, unit_id, user_role):\n        \"\"\"\n        Removes a training unit by ID if the user has the appropriate role.\n        \"\"\"\n        if user_role in ['admin', 'instructor']:\n            if unit_id in self.units:\n                del self.units[unit_id]\n                return True\n            else:\n                raise ValueError(\"Training unit not found\")\n        else:\n            raise PermissionError(\"User does not have permission to remove training units\")\n\n# Example usage:\nmanager = TrainingUnitManager()\nmanager.units['123'] = \"Python Basics\"\ntry:\n    manager.remove_training_unit('123', 'admin')\n    print(\"Training unit removed successfully.\")\nexcept Exception as e:\n    print(e)",
  "g_79": "def bind_exercise_to_workout(exercise_id, workout_id):\n    \"\"\"\n    Bind an exercise to a workout via the API.\n    \"\"\"\n    # Assuming there's an API client named `api_client`\n    response = api_client.post(\n        '/workouts/{}/exercises'.format(workout_id),\n        json={'exercise_id': exercise_id}\n    )\n    return response.json()",
  "g_80": "def detach_exercise_from_training_unit(exercise_id, training_unit_id):\n    \"\"\"\n    Detaches an exercise from a training unit.\n    \n    Args:\n        exercise_id (int): The ID of the exercise to be detached.\n        training_unit_id (int): The ID of the training unit from which the exercise will be detached.\n        \n    Returns:\n        bool: True if the detachment was successful, False otherwise.\n    \"\"\"\n    # Simulate database operation\n    try:\n        # Logic to detach exercise from training unit\n        print(f\"Detaching exercise {exercise_id} from training unit {training_unit_id}\")\n        return True\n    except Exception as e:\n        print(f\"Failed to detach exercise: {e}\")\n        return False",
  "g_81": "class UserRegistration:\n    def __init__(self, users):\n        self.users = users\n\n    def register_user(self, email, password):\n        \"\"\"\n        Register a new user with the given email and password.\n        Raises ValueError if the email is already taken.\n        \"\"\"\n        if email in self.users:\n            raise ValueError(\"Email already registered\")\n        self.users[email] = password",
  "g_82": "from flask import Flask, request, jsonify\nfrom functools import wraps\n\napp = Flask(__name__)\n\n# Mock database of users\nusers = {\n    1: {\"name\": \"Alice\", \"admin\": True},\n    2: {\"name\": \"Bob\", \"admin\": False}\n}\n\ndef admin_required(f):\n    @wraps(f)\n    def decorated_function(*args, **kwargs):\n        if not request.headers.get('Authorization') == 'Bearer admin_token':\n            return jsonify({\"error\": \"Admin access required\"}), 403\n        return f(*args, **kwargs)\n    return decorated_function\n\n@app.route('/users/<int:user_id>', methods=['DELETE'])\n@admin_required\ndef delete_user(user_id):\n    \"\"\"\n    Deletes a user account by ID.\n    Only admins can perform this action.\n    \"\"\"\n    if user_id in users:\n        del users[user_id]\n        return jsonify({\"message\": \"User deleted successfully\"}), 200\n    else:\n        return jsonify({\"error\": \"User not found\"}), 404\n\nif __name__ == '__main__':\n    app.run(debug=True)",
  "g_83": "def update_user_account(user_id, new_data):\n    \"\"\"\n    Update user account data via API.\n\n    :param user_id: ID of the user to update.\n    :param new_data: Dictionary containing new data for the user.\n    :return: Response from the API.\n    \"\"\"\n    import requests\n    url = f\"https://api.example.com/users/{user_id}\"\n    headers = {\"Authorization\": \"Bearer your_access_token\"}\n    response = requests.put(url, json=new_data, headers=headers)\n    return response.json()",
  "g_84": "class CRUDBase:\n    \"\"\"Base class implementing common CRUD operations.\"\"\"\n\n    def create(self, data):\n        raise NotImplementedError(\"Create method must be implemented\")\n\n    def read(self, entity_id):\n        raise NotImplementedError(\"Read method must be implemented\")\n\n    def update(self, entity_id, data):\n        raise NotImplementedError(\"Update method must be implemented\")\n\n    def delete(self, entity_id):\n        raise NotImplementedError(\"Delete method must be implemented\")",
  "g_85": "class Repository:\n    def __init__(self, model):\n        self.model = model\n\n    def get_model(self):\n        \"\"\"Return the ORM model associated with this repository.\"\"\"\n        return self.model",
  "g_86": "class GenericRepository:\n    def __init__(self, session):\n        self.session = session\n\n    def remove(self, model):\n        \"\"\"\n        Removes a record from the database.\n        \"\"\"\n        self.session.delete(model)\n        self.session.commit()",
  "g_87": "def get_training_blocks(plan_id):\n    \"\"\"\n    Retrieve a list of training blocks that are part of the given training plan.\n    \n    Args:\n        plan_id (int): The ID of the training plan.\n        \n    Returns:\n        list: A list of training block objects.\n    \"\"\"\n    # Assuming there is a function `get_training_blocks_for_plan` in the backend\n    return get_training_blocks_for_plan(plan_id)",
  "g_88": "class UserAccount:\n    def __init__(self, is_active):\n        self.is_active = is_active\n\n    def is_account_active(self):\n        \"\"\"Check if the user account is active.\"\"\"\n        return self.is_active",
  "g_89": "def mark_user_inactive(user_id):\n    \"\"\"\n    Marks a user as inactive due to soft account deactivation.\n    \"\"\"\n    # Assuming there's a User model with an 'is_active' field\n    from models import User\n    \n    user = User.query.get(user_id)\n    if user:\n        user.is_active = False\n        user.save()",
  "g_90": "class DifficultyLevelMapper:\n    \"\"\"Maps difficulty levels to their corresponding database table.\"\"\"\n\n    def get_table_name(self, difficulty_level):\n        return f\"difficulty_levels_{difficulty_level}\"",
  "g_91": "class TrainingBlock(models.Model):\n    \"\"\"ORM model for training blocks.\"\"\"\n    name = models.CharField(max_length=100)\n    description = models.TextField()\n    duration_minutes = models.IntegerField()\n\n    def __str__(self):\n        return self.name",
  "g_92": "from sqlalchemy import Column, Integer, String\nfrom sqlalchemy.ext.declarative import declarative_base\n\nBase = declarative_base()\n\nclass User(Base):\n    __tablename__ = 'users'\n    \n    id = Column(Integer, primary_key=True)\n    username = Column(String, unique=True, nullable=False)\n    email = Column(String, unique=True, nullable=False)\n    password_hash = Column(String, nullable=False)",
  "g_93": "class BodyPart:\n    \"\"\"Represents a body part with an identifier from the database.\"\"\"\n    \n    def __init__(self, id: int, name: str):\n        self.id = id\n        self.name = name",
  "g_94": "class ResponseSchema:\n    \"\"\"Defines the structure of the response schema for a body part.\"\"\"\n    \n    def __init__(self, name, description, measurements):\n        self.name = name\n        self.description = description\n        self.measurements = measurements\n\n    def to_dict(self):\n        return {\n            \"name\": self.name,\n            \"description\": self.description,\n            \"measurements\": self.measurements\n        }",
  "g_95": "class ExerciseUpdateSchema:\n    \"\"\"Schema for updating exercise data.\"\"\"\n    \n    name = str\n    description = str\n    duration = int\n    intensity = float\n\nThis class defines a schema for updating exercise data, including fields for the exercise's name, description, duration, and intensity.",
  "g_96": "class ExerciseSchema:\n    \"\"\"Describes the schema for an exercise in API responses.\"\"\"\n    \n    def __init__(self, id, name, description, difficulty):\n        self.id = id\n        self.name = name\n        self.description = description\n        self.difficulty = difficulty\n\n    def to_dict(self):\n        return {\n            \"id\": self.id,\n            \"name\": self.name,\n            \"description\": self.description,\n            \"difficulty\": self.difficulty\n        }",
  "g_97": "class ExerciseResponse:\n    \"\"\"Fields returned to the client for an exercise type.\"\"\"\n    \n    def __init__(self, id: int, name: str, description: str, duration: int):\n        self.id = id\n        self.name = name\n        self.description = description\n        self.duration = duration",
  "g_98": "class DifficultyLevel:\n    \"\"\"Represents a difficulty level in the database.\"\"\"\n    \n    def __init__(self, id, name):\n        self.id = id\n        self.name = name\n\n    @classmethod\n    def from_db(cls, db_connection, level_id):\n        cursor = db_connection.cursor()\n        query = \"SELECT id, name FROM difficulty_levels WHERE id = %s\"\n        cursor.execute(query, (level_id,))\n        result = cursor.fetchone()\n        if result:\n            return cls(*result)\n        return None",
  "g_99": "class TrainingPlanSchema:\n    \"\"\"Represents the schema for a training plan with fields from the database.\"\"\"\n\n    def __init__(self, id: int, user_id: int, name: str, description: str):\n        self.id = id\n        self.user_id = user_id\n        self.name = name\n        self.description = description\n\n    @classmethod\n    def from_db_record(cls, record):\n        \"\"\"Creates an instance of TrainingPlanSchema from a database record.\"\"\"\n        return cls(\n            id=record['id'],\n            user_id=record['user_id'],\n            name=record['name'],\n            description=record['description']\n        )",
  "g_100": "class TrainingUnitSchema:\n    \"\"\"Base fields for a training unit schema.\"\"\"\n    \n    id = IntegerField(primary_key=True)\n    title = StringField(required=True)\n    description = StringField()\n    duration_minutes = IntegerField(default=0)\n    created_at = DateTimeField(auto_now_add=True)\n    updated_at = DateTimeField(auto_now=True)",
  "g_101": "class UserRegistrationSchema:\n    \"\"\"\n    Schema for user registration input.\n    \"\"\"\n    username: str\n    email: str\n    password: str\n    confirm_password: str\n\n    def validate(self):\n        if self.password != self.confirm_password:\n            raise ValueError(\"Passwords do not match\")\n\nThis Python class defines a schema for user registration, including fields for username, email, and passwords. It also includes a method to validate that the passwords match before allowing registration.",
  "g_102": "class UserSchema:\n    \"\"\"Schema for representing user data.\"\"\"\n    def __init__(self, id, username, email):\n        self.id = id\n        self.username = username\n        self.email = email",
  "g_103": "class User:\n    \"\"\"Represents a user with hashed password for internal use.\"\"\"\n    \n    def __init__(self, username: str, password_hash: str):\n        self.username = username\n        self.password_hash = password_hash",
  "g_104": "class UserUpdateSchema:\n    \"\"\"\n    Schema for updating user fields.\n    \"\"\"\n    username = str\n    email = str\n    age = int\n    is_active = bool\n\nThis class defines a schema with fields that are typically allowed to be updated when modifying a user.",
  "g_105": "from sqlalchemy.orm import Session\nfrom myapp.models import MyModel\nfrom myapp.schemas import MySchema\n\ndef create_record(db: Session, item: MySchema):\n    \"\"\"Persist a new record built from a Pydantic schema.\"\"\"\n    db_item = MyModel(**item.dict())\n    db.add(db_item)\n    db.commit()\n    db.refresh(db_item)\n    return db_item",
  "g_106": "class BaseRepository:\n    def partial_update(self, model_id, updates):\n        \"\"\"\n        Partially update fields of a record in the database.\n        \"\"\"\n        self.session.query(model).filter(model.id == model_id).update(updates)\n        self.session.commit()",
  "g_107": "class RecordFetcher:\n    def __init__(self, db_connection):\n        self.db_connection = db_connection\n\n    def fetch_paginated_records(self, owner_id, page_number, page_size):\n        \"\"\"\n        Fetches paginated records for a specific owner.\n        \n        :param owner_id: ID of the owner whose records to fetch\n        :param page_number: Page number (1-based)\n        :param page_size: Number of records per page\n        :return: List of records belonging to the owner\n        \"\"\"\n        offset = (page_number - 1) * page_size\n        query = \"SELECT * FROM records WHERE owner_id = %s LIMIT %s OFFSET %s\"\n        cursor = self.db_connection.cursor()\n        cursor.execute(query, (owner_id, page_size, offset))\n        records = cursor.fetchall()\n        cursor.close()\n        return records",
  "g_108": "class TrainingPlanRepository:\n    \"\"\"Provides methods for database operations related to training plans.\"\"\"\n\n    def __init__(self, db_connection):\n        self.db_connection = db_connection\n\n    def get_training_plan(self, plan_id):\n        \"\"\"Retrieve a training plan by its ID.\"\"\"\n        with self.db_connection.cursor() as cursor:\n            cursor.execute(\"SELECT * FROM training_plans WHERE id = %s\", (plan_id,))\n            return cursor.fetchone()\n\n    def create_training_plan(self, plan_data):\n        \"\"\"Create a new training plan in the database.\"\"\"\n        with self.db_connection.cursor() as cursor:\n            cursor.execute(\n                \"INSERT INTO training_plans (name, description) VALUES (%s, %s) RETURNING id\",\n                (plan_data['name'], plan_data['description']),\n            )\n            return cursor.fetchone()['id']\n\n    def update_training_plan(self, plan_id, plan_data):\n        \"\"\"Update an existing training plan in the database.\"\"\"\n        with self.db_connection.cursor() as cursor:\n            cursor.execute(\n                \"UPDATE training_plans SET name = %s, description = %s WHERE id = %s\",\n                (plan_data['name'], plan_data['description'], plan_id),\n            )\n\n    def delete_training_plan(self, plan_id):\n        \"\"\"Delete a training plan from the database.\"\"\"\n        with self.db_connection.cursor() as cursor:\n            cursor.execute(\"DELETE FROM training_plans WHERE id = %s\", (plan_id,))",
  "g_109": "class TrainingBlock:\n    def __init__(self, plan):\n        self.plan = plan\n\n    def detach_from_plan(self):\n        \"\"\"Detach the training block from its associated plan.\"\"\"\n        self.plan.remove_training_block(self)\n        self.plan = None\n\nThis Python class `TrainingBlock` represents a training block that can be attached to a plan. The method `detach_from_plan` is used to remove the training block from its associated plan, effectively detaching it.",
  "g_110": "class ExerciseManager:\n    def __init__(self, db_connection):\n        self.db_connection = db_connection\n\n    def add_exercise_to_block(self, block_id, exercise_data):\n        \"\"\"Add an exercise to a training block.\"\"\"\n        with self.db_connection.cursor() as cursor:\n            query = \"INSERT INTO exercises (block_id, name, description) VALUES (%s, %s, %s)\"\n            cursor.execute(query, (block_id, exercise_data['name'], exercise_data['description']))\n            self.db_connection.commit()\n\n    def get_exercises_by_block(self, block_id):\n        \"\"\"Get all exercises for a specific training block.\"\"\"\n        with self.db_connection.cursor() as cursor:\n            query = \"SELECT * FROM exercises WHERE block_id = %s\"\n            cursor.execute(query, (block_id,))\n            return cursor.fetchall()",
  "g_111": "def add_exercise_to_block(repo_path, block_id, exercise_data):\n    \"\"\"\n    Adds an exercise to a block in the repository without duplicating content.\n    \"\"\"\n    # Implementation goes here",
  "g_112": "def remove_exercise_from_training_block(data_access_layer, training_block_id, exercise_id):\n    \"\"\"\n    Removes an exercise from a training block in the data access layer.\n    \"\"\"\n    data_access_layer.remove_exercise_from_block(training_block_id, exercise_id)",
  "g_113": "class UserRepository:\n    \"\"\"Manages user-specific queries like lookup by email and authentication.\"\"\"\n\n    def __init__(self, db_connection):\n        self.db_connection = db_connection\n\n    def find_by_email(self, email):\n        \"\"\"Finds a user by their email address.\"\"\"\n        query = \"SELECT * FROM users WHERE email = %s\"\n        result = self.db_connection.execute(query, (email,))\n        return result.fetchone()\n\n    def authenticate(self, email, password):\n        \"\"\"Authenticates a user with the given email and password.\"\"\"\n        query = \"SELECT * FROM users WHERE email = %s AND password = %s\"\n        result = self.db_connection.execute(query, (email, password))\n        return result.fetchone() is not None",
  "g_114": "from sqlalchemy import Column, Integer, String, ForeignKey\nfrom sqlalchemy.orm import relationship\nfrom sqlalchemy.ext.declarative import declarative_base\n\nBase = declarative_base()\n\nclass Exercise(Base):\n    __tablename__ = 'exercises'\n    \n    id = Column(Integer, primary_key=True)\n    name = Column(String, nullable=False)\n    description = Column(String)\n    category_id = Column(Integer, ForeignKey('categories.id'))\n    \n    category = relationship(\"Category\", back_populates=\"exercises\")\n\nclass Category(Base):\n    __tablename__ = 'categories'\n    \n    id = Column(Integer, primary_key=True)\n    name = Column(String, nullable=False)\n    \n    exercises = relationship(\"Exercise\", back_populates=\"category\")",
  "g_115": "import jwt\nfrom datetime import datetime, timedelta\n\ndef issue_jwt(user_id):\n    \"\"\"\n    Issues a JWT for the given user ID.\n    \"\"\"\n    payload = {\n        'user_id': user_id,\n        'exp': datetime.utcnow() + timedelta(hours=1)\n    }\n    secret_key = 'your_secret_key_here'\n    token = jwt.encode(payload, secret_key, algorithm='HS256')\n    return token",
  "g_116": "from flask import Flask, jsonify\n\napp = Flask(__name__)\n\n# Simulated database of body parts\nbody_parts = {\n    1: {\"id\": 1, \"name\": \"Head\"},\n    2: {\"id\": 2, \"name\": \"Torso\"},\n    3: {\"id\": 3, \"name\": \"Arms\"}\n}\n\n@app.route('/body_part/<int:part_id>', methods=['GET'])\ndef get_body_part(part_id):\n    \"\"\"\n    Fetch a single body part by ID and respond with JSON.\n    \"\"\"\n    part = body_parts.get(part_id)\n    if part:\n        return jsonify(part), 200\n    else:\n        return jsonify({\"error\": \"Body part not found\"}), 404\n\nif __name__ == '__main__':\n    app.run(debug=True)",
  "g_117": "def locate_body_part_by_name(body_parts, part_name):\n    \"\"\"\n    Locate a body part by its name in a list of body parts.\n    \n    :param body_parts: List of dictionaries, each representing a body part with keys 'name' and 'location'\n    :param part_name: Name of the body part to locate\n    :return: Dictionary representing the located body part or None if not found\n    \"\"\"\n    return next((part for part in body_parts if part['name'] == part_name), None)",
  "g_118": "class BodyPart:\n    def __init__(self):\n        self.parts = {}\n\n    def add_part(self, name, description):\n        if name in self.parts:\n            raise ValueError(f\"Duplicate part name: {name}\")\n        self.parts[name] = description\n\n# Example usage:\nbody = BodyPart()\ntry:\n    body.add_part(\"head\", \"The head of the body\")\n    body.add_part(\"head\", \"Another head\")  # This will raise an error\nexcept ValueError as e:\n    print(e)",
  "g_119": "from flask import Flask, request, jsonify\nfrom functools import wraps\n\napp = Flask(__name__)\n\ndef requires_admin(f):\n    @wraps(f)\n    def decorated_function(*args, **kwargs):\n        if not request.headers.get('Authorization') == 'admin_token':\n            return jsonify({'error': 'Unauthorized'}), 403\n        return f(*args, **kwargs)\n    return decorated_function\n\n@app.route('/body_parts/<int:part_id>', methods=['DELETE'])\n@requires_admin\ndef delete_body_part(part_id):\n    \"\"\"\n    Deletes a body part by ID.\n    Only admins are allowed to call this endpoint.\n    \"\"\"\n    # Logic to delete the body part from the database\n    return jsonify({'message': 'Body part deleted successfully'}), 200\n\nif __name__ == '__main__':\n    app.run(debug=True)",
  "g_120": "def update_body_part(api_client, part_id, new_data):\n    \"\"\"\n    Update an existing body part through the API.\n    \"\"\"\n    response = api_client.put(f'/body_parts/{part_id}', json=new_data)\n    response.raise_for_status()\n    return response.json()",
  "g_121": "from flask import Flask, jsonify, abort\n\napp = Flask(__name__)\n\nexercises = {\n    1: {\"id\": 1, \"name\": \"Push-ups\", \"description\": \"Perform push-ups.\"},\n    2: {\"id\": 2, \"name\": \"Squats\", \"description\": \"Stand and lower into a squat position.\"}\n}\n\n@app.route('/exercise/<int:exercise_id>', methods=['GET'])\ndef get_exercise(exercise_id):\n    \"\"\"\n    Return an exercise by its ID or 404 if it is missing.\n    \"\"\"\n    exercise = exercises.get(exercise_id)\n    if exercise:\n        return jsonify(exercise)\n    else:\n        abort(404, description=f\"Exercise with id {exercise_id} not found.\")",
  "g_122": "class ExerciseManager:\n    def __init__(self):\n        self.exercises = []\n\n    def add_exercise(self, exercise_name, creator):\n        if not self.is_creator_allowed(creator):\n            raise PermissionError(\"User is not allowed to create exercises.\")\n        self.exercises.append(exercise_name)\n        return f\"Exercise '{exercise_name}' added by {creator}.\"\n\n    def is_creator_allowed(self, creator):\n        # Implement logic to check if the user has permission to create exercises\n        allowed_creators = [\"admin\", \"instructor\"]\n        return creator in allowed_creators",
  "g_123": "def update_exercise(exercise_id, new_data):\n    \"\"\"\n    Update an existing exercise through the API.\n\n    :param exercise_id: ID of the exercise to be updated.\n    :param new_data: Dictionary containing the new data for the exercise.\n    :return: Response from the API.\n    \"\"\"\n    url = f\"https://api.example.com/exercises/{exercise_id}\"\n    headers = {\"Content-Type\": \"application/json\"}\n    response = requests.put(url, json=new_data, headers=headers)\n    return response.json()",
  "g_124": "def remove_exercise_by_id(exercise_id, user):\n    \"\"\"\n    Removes an exercise by its ID if the user has permission.\n    \"\"\"\n    if user.has_permission('remove_exercise'):\n        # Logic to remove the exercise from the database or storage\n        pass",
  "g_125": "class ExerciseTypeService:\n    def get_exercise_type_by_id(self, exercise_type_id):\n        \"\"\"\n        Retrieves an exercise type by its ID.\n        \"\"\"\n        # Simulate database retrieval\n        exercise_types = {\n            1: \"Cardio\",\n            2: \"Strength Training\",\n            3: \"Flexibility\"\n        }\n        return exercise_types.get(exercise_type_id, None)",
  "g_126": "def get_exercise_type(exercise_name):\n    \"\"\"\n    Returns the type of exercise based on its name.\n    \"\"\"\n    exercise_types = {\n        \"push-ups\": \"strength\",\n        \"sit-ups\": \"core\",\n        \"jumps\": \"cardio\",\n        # Add more exercises and types as needed\n    }\n    return exercise_types.get(exercise_name.lower(), \"unknown\")",
  "g_127": "from flask import Flask, request, jsonify\n\napp = Flask(__name__)\n\nclass ExerciseType:\n    def __init__(self, name):\n        self.name = name\n\nexercise_types = {}\n\n@app.route('/exercises', methods=['POST'])\ndef create_exercise_type():\n    \"\"\"\n    Create a new exercise type.\n    Returns 201 Created on success, 409 Conflict if the name already exists.\n    \"\"\"\n    data = request.get_json()\n    name = data.get('name')\n    \n    if name in exercise_types:\n        return jsonify({'error': 'Exercise type with this name already exists'}), 409\n    \n    new_type = ExerciseType(name)\n    exercise_types[name] = new_type\n    return jsonify({'message': f'Exercise type {name} created successfully'}), 201\n\nif __name__ == '__main__':\n    app.run(debug=True)",
  "g_128": "def update_exercise_type_by_id(exercise_id, new_data):\n    \"\"\"\n    Update an exercise type by its ID through the API.\n    \n    :param exercise_id: int - The ID of the exercise type to update.\n    :param new_data: dict - A dictionary containing the new data for the exercise type.\n    :return: dict - The updated exercise type data.\n    \"\"\"\n    # Simulate an API call to update the exercise type\n    response = {\n        \"id\": exercise_id,\n        **new_data\n    }\n    return response",
  "g_129": "class ExerciseManager:\n    def __init__(self):\n        self.exercises = {}\n        self.permissions = {}\n\n    def add_exercise(self, exercise_id, details):\n        \"\"\"Add a new exercise with the given ID and details.\"\"\"\n        self.exercises[exercise_id] = details\n\n    def remove_exercise(self, exercise_id, user_id):\n        \"\"\"Remove an exercise if the user has the necessary permissions.\"\"\"\n        if exercise_id in self.exercises:\n            if user_id in self.permissions.get(exercise_id, []):\n                del self.exercises[exercise_id]\n                return True\n            else:\n                raise PermissionError(\"User does not have permission to remove this exercise.\")\n        else:\n            raise KeyError(\"Exercise not found.\")\n\n    def grant_permission(self, exercise_id, user_id):\n        \"\"\"Grant a user permission to modify an exercise.\"\"\"\n        if exercise_id in self.exercises:\n            if exercise_id not in self.permissions:\n                self.permissions[exercise_id] = []\n            self.permissions[exercise_id].append(user_id)\n        else:\n            raise KeyError(\"Exercise not found.\")",
  "g_130": "from flask import Flask, request, jsonify\n\napp = Flask(__name__)\n\nclass DifficultyLevel:\n    def __init__(self, id, name):\n        self.id = id\n        self.name = name\n\ndifficulty_levels = []\n\n@app.route('/api/difficulty-levels', methods=['POST'])\ndef add_difficulty_level():\n    \"\"\"\n    Adds a new difficulty level.\n    \n    Only admins can create new difficulty levels.\n    \"\"\"\n    data = request.get_json()\n    if not data or 'name' not in data:\n        return jsonify({'error': 'Invalid request'}), 400\n    \n    name = data['name']\n    id = len(difficulty_levels) + 1\n    new_difficulty_level = DifficultyLevel(id, name)\n    difficulty_levels.append(new_difficulty_level)\n    \n    return jsonify({'id': new_difficulty_level.id, 'name': new_difficulty_level.name}), 201\n\nif __name__ == '__main__':\n    app.run(debug=True)",
  "g_131": "def remove_difficulty_level(difficulties, level_id):\n    \"\"\"\n    Removes a difficulty level by its ID from the list of difficulties.\n    \n    :param difficulties: List of dictionaries representing difficulty levels.\n    :param level_id: ID of the difficulty level to be removed.\n    :return: None\n    \"\"\"\n    difficulties[:] = [level for level in difficulties if level['id'] != level_id]",
  "g_132": "def update_difficulty_level(difficulty_id, new_level):\n    \"\"\"\n    Update the difficulty level of a game by its ID.\n    \n    Args:\n        difficulty_id (int): The ID of the difficulty level to update.\n        new_level (str): The new difficulty level ('easy', 'medium', 'hard').\n        \n    Returns:\n        bool: True if the update was successful, False otherwise.\n    \"\"\"\n    # Simulate database update\n    difficulties = {\n        1: 'easy',\n        2: 'medium',\n        3: 'hard'\n    }\n    \n    if difficulty_id in difficulties:\n        difficulties[difficulty_id] = new_level\n        return True\n    else:\n        return False",
  "g_133": "def get_training_plan_by_id(plan_id):\n    \"\"\"\n    Retrieve the training plan by its identifier.\n    \"\"\"\n    # Assuming a dictionary to simulate database access\n    training_plans = {\n        1: {\"name\": \"Cardio\", \"duration\": \"30 minutes\"},\n        2: {\"name\": \"Strength Training\", \"duration\": \"60 minutes\"}\n    }\n    return training_plans.get(plan_id, None)",
  "g_134": "def find_training_plan_by_name(plan_name):\n    \"\"\"\n    Find a training plan by its name.\n\n    Args:\n        plan_name (str): The name of the training plan to search for.\n\n    Returns:\n        dict: The training plan if found, otherwise None.\n    \"\"\"\n    # Assuming training_plans is a dictionary where keys are plan names and values are plan details\n    return next((plan for plan in training_plans.values() if plan['name'] == plan_name), None)",
  "g_135": "class TrainingPlanSearch:\n    def __init__(self, db_session):\n        self.db_session = db_session\n\n    def search_plans_by_name(self, query):\n        \"\"\"\n        Search for training plans by name across all owners.\n        \"\"\"\n        return self.db_session.query(TrainingPlan).filter(\n            TrainingPlan.name.ilike(f'%{query}%')\n        ).all()",
  "g_136": "class TrainingPlan:\n    def update(self, user_id, new_plan):\n        \"\"\"\n        Update an existing training plan for a user.\n        \n        :param user_id: ID of the user whose plan is being updated.\n        :param new_plan: Dictionary containing the new training plan details.\n        :return: True if the update was successful, False otherwise.\n        \"\"\"\n        # Simulate updating the training plan in a database\n        try:\n            # Logic to update the plan in the database\n            pass\n        except Exception as e:\n            print(f\"Failed to update training plan for user {user_id}: {e}\")\n            return False\n        return True",
  "g_137": "def detach_training_block_from_plan(plan_id, block_id):\n    \"\"\"\n    Detach a training block from a workout plan via the API.\n    \"\"\"\n    import requests\n\n    url = f\"https://api.example.com/plans/{plan_id}/blocks/{block_id}\"\n    response = requests.delete(url)\n    \n    if response.status_code == 204:\n        return \"Block detached successfully.\"\n    else:\n        raise Exception(f\"Failed to detach block: {response.text}\")",
  "g_138": "from flask import Flask, jsonify\n\napp = Flask(__name__)\n\nclass TrainingUnit:\n    def __init__(self, id, name):\n        self.id = id\n        self.name = name\n\nclass TrainingPlan:\n    def __init__(self, id, units):\n        self.id = id\n        self.units = units\n\n# Sample data\ntraining_plans = {\n    1: TrainingPlan(1, [TrainingUnit(101, \"Warm-up\"), TrainingUnit(102, \"Cardio\")]),\n    2: TrainingPlan(2, [TrainingUnit(201, \"Strength training\"), TrainingUnit(202, \"Flexibility\")])\n}\n\n@app.route('/api/training_plans/<int:plan_id>/units', methods=['GET'])\ndef list_training_units(plan_id):\n    \"\"\"\n    Lists the training units contained in a training plan.\n    \"\"\"\n    plan = training_plans.get(plan_id)\n    if not plan:\n        return jsonify({\"error\": \"Training plan not found\"}), 404\n    return jsonify([{\"id\": unit.id, \"name\": unit.name} for unit in plan.units])\n\nif __name__ == '__main__':\n    app.run(debug=True)",
  "g_139": "def get_training_unit_by_id(unit_id):\n    \"\"\"\n    Returns a training unit by its ID.\n\n    Args:\n        unit_id (int): The ID of the training unit to retrieve.\n\n    Returns:\n        dict: A dictionary representing the training unit.\n    \"\"\"\n    # Simulate fetching data from a database or API\n    training_units = {\n        1: {\"id\": 1, \"title\": \"Introduction to Python\", \"content\": \"Basic concepts and syntax.\"},\n        2: {\"id\": 2, \"title\": \"Advanced Python\", \"content\": \"Object-oriented programming and more.\"},\n    }\n    return training_units.get(unit_id, None)",
  "g_140": "def get_training_block_by_name(block_name):\n    \"\"\"\n    Retrieve the training block by its name.\n\n    :param block_name: The name of the training block to retrieve.\n    :return: The training block if found, otherwise None.\n    \"\"\"\n    # Assuming training_blocks is a dictionary where keys are block names and values are block objects\n    return training_blocks.get(block_name)",
  "g_141": "def list_training_units(name):\n    \"\"\"\n    List training units that match the given name.\n\n    Args:\n        name (str): The name to search for in training unit titles.\n\n    Returns:\n        list: A list of dictionaries representing matching training units.\n    \"\"\"\n    # Assuming a database session and a TrainingUnit model\n    from models import TrainingUnit\n\n    return [\n        unit.to_dict()\n        for unit in TrainingUnit.query.filter(TrainingUnit.title.contains(name)).all()\n    ]",
  "g_142": "class TrainingBlock:\n    def __init__(self, name):\n        self.name = name\n\ndef create_training_block(name):\n    \"\"\"\n    Create a new training block with the given name.\n    Raises ValueError if a block with the same name already exists.\n    \"\"\"\n    if name in existing_blocks:\n        raise ValueError(f\"Training block '{name}' already exists.\")\n    new_block = TrainingBlock(name)\n    existing_blocks[name] = new_block\n    return new_block\n\nexisting_blocks = {}",
  "g_143": "class TrainingUnit:\n    def update(self, data):\n        \"\"\"\n        Update the training unit with new data.\n\n        :param data: Dictionary containing the new data to be updated.\n        \"\"\"\n        # Simulate updating the training unit in a database or other storage\n        for key, value in data.items():\n            setattr(self, key, value)",
  "g_144": "class TrainingBlock:\n    def __init__(self, block_id, owner):\n        self.block_id = block_id\n        self.owner = owner\n\ndef delete_training_block(block_id, current_user):\n    \"\"\"\n    Deletes a training block by ID if the current user is the owner.\n    \"\"\"\n    # Assuming blocks is a list of TrainingBlock instances\n    global blocks\n    \n    for block in blocks:\n        if block.block_id == block_id and block.owner == current_user:\n            blocks.remove(block)\n            return True\n    return False",
  "g_145": "def attach_exercise_to_training_unit(training_unit_id, exercise_id):\n    \"\"\"\n    Attaches an exercise to a training unit.\n    \n    Args:\n        training_unit_id (int): The ID of the training unit.\n        exercise_id (int): The ID of the exercise to attach.\n        \n    Returns:\n        bool: True if the attachment was successful, False otherwise.\n    \"\"\"\n    # Simulate database operation\n    try:\n        # Logic to attach exercise to training unit\n        pass\n    except Exception as e:\n        print(f\"Error attaching exercise {exercise_id} to training unit {training_unit_id}: {e}\")\n        return False\n    return True",
  "g_146": "class TrainingUnit:\n    def __init__(self, exercises):\n        self.exercises = exercises\n\n    def list_exercises(self):\n        \"\"\"List all exercises in the training unit.\"\"\"\n        return self.exercises",
  "g_147": "def remove_exercise_from_training_block(training_block_id, exercise_id):\n    \"\"\"\n    Removes an exercise from a training block via the API.\n    \"\"\"\n    import requests\n\n    url = f\"https://api.example.com/training_blocks/{training_block_id}/exercises/{exercise_id}\"\n    response = requests.delete(url)\n    \n    if response.status_code == 204:\n        return \"Exercise removed successfully.\"\n    else:\n        return f\"Failed to remove exercise. Status code: {response.status_code}\"",
  "g_148": "class UserAPI:\n    def __init__(self, users):\n        self.users = {user.email: user for user in users}\n\n    def get_user_by_email(self, email):\n        \"\"\"\n        Look up a user by their e-mail address.\n        \"\"\"\n        return self.users.get(email)",
  "g_149": "from flask import Flask, request, jsonify\n\napp = Flask(__name__)\n\nusers = {}\n\n@app.route('/register', methods=['POST'])\ndef register_user():\n    \"\"\"\n    Registers a new user. Returns 409 if the email is already in use.\n    \"\"\"\n    data = request.get_json()\n    email = data.get('email')\n    \n    if email in users:\n        return jsonify({'error': 'Email already registered'}), 409\n    \n    users[email] = {\n        'username': data.get('username'),\n        'password': data.get('password')  # In practice, hash this\n    }\n    \n    return jsonify({'message': 'User registered successfully'}), 201\n\nif __name__ == '__main__':\n    app.run(debug=True)",
  "g_150": "def delete_user(user_id):\n    \"\"\"\n    Deletes the user account with the given ID.\n    \"\"\"\n    # Implementation to delete a user by ID goes here\n    pass",
  "g_151": "class UserAccount:\n    def update(self, user_id, new_data):\n        \"\"\"\n        Update an existing user account with new data.\n        \"\"\"\n        # Simulate updating a user in a database\n        print(f\"Updating user {user_id} with data: {new_data}\")\n        return {\"status\": \"success\", \"message\": \"User updated successfully\"}",
  "g_152": "class DevelopmentSettings:\n    \"\"\"Configuration class for the development environment.\"\"\"\n    \n    def __init__(self):\n        self.env_file = \".env.development\"\n        self.database_url = \"sqlite:///dev.db\"\n        self.debug_mode = True\n\n    def load_env(self):\n        \"\"\"Load environment variables from the .env file.\"\"\"\n        with open(self.env_file, 'r') as file:\n            for line in file:\n                key, value = line.strip().split('=')\n                setattr(self, key.strip(), value.strip())",
  "g_153": "class TestConfig:\n    \"\"\"Configuration for testing environment.\"\"\"\n    \n    DATABASE_URL = \"sqlite:///test.db\"\n    SECRET_KEY = \"test_secret_key\"\n    DEBUG = True\n\ndef load_env_file(file_path):\n    \"\"\"Load environment variables from a file.\"\"\"\n    with open(file_path, 'r') as file:\n        for line in file:\n            key, value = line.strip().split('=')\n            os.environ[key] = value",
  "g_154": "class LocalConfig:\n    \"\"\"Configuration for local development environment.\"\"\"\n    \n    def __init__(self):\n        self.env_file = \".env.local\"\n        self.database_url = \"sqlite:///local.db\"\n\n    @property\n    def settings(self):\n        return {\n            \"env\": \"development\",\n            \"debug\": True,\n            \"database_url\": self.database_url,\n        }",
  "g_155": "class DatabaseOperations:\n    \"\"\"Base class for database read and write operations.\"\"\"\n    \n    def __init__(self, connection):\n        self.connection = connection\n\n    def read(self, query):\n        with self.connection.cursor() as cursor:\n            cursor.execute(query)\n            return cursor.fetchall()\n\n    def write(self, query, data=None):\n        with self.connection.cursor() as cursor:\n            if data:\n                cursor.execute(query, data)\n            else:\n                cursor.execute(query)\n            self.connection.commit()"
}