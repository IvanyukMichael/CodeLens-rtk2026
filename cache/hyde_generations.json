{
  "q_01": "import jwt\nfrom datetime import datetime, timedelta\n\ndef create_access_token(user_id, secret_key):\n    \"\"\"\n    Creates an access token with a 15-minute expiration time.\n    \"\"\"\n    payload = {\n        'user_id': user_id,\n        'exp': datetime.utcnow() + timedelta(minutes=15)\n    }\n    return jwt.encode(payload, secret_key, algorithm='HS256')",
  "q_02": "import jwt\n\ndef verify_jwt_token(token, secret_key):\n    \"\"\"\n    Verifies a JWT token and returns the decoded payload if valid.\n    \n    :param token: JWT token as a string\n    :param secret_key: Secret key used to sign the JWT\n    :return: Decoded payload of the JWT or raises an exception if invalid\n    \"\"\"\n    try:\n        return jwt.decode(token, secret_key, algorithms=[\"HS256\"])\n    except jwt.ExpiredSignatureError:\n        raise ValueError(\"Token has expired\")\n    except jwt.InvalidTokenError:\n        raise ValueError(\"Invalid token\")",
  "q_03": "def is_superuser(user):\n    \"\"\"Check if the user is a superuser.\"\"\"\n    return user.is_superuser",
  "q_04": "def create_account(email, password):\n    \"\"\"\n    Creates a new user account with the given email and password.\n    Returns a success message or an error if the email is already taken.\n    \"\"\"\n    if User.objects.filter(email=email).exists():\n        return \"Email already taken\"\n    else:\n        User.objects.create_user(email=email, password=password)\n        return \"Account created successfully\"",
  "q_05": "import hashlib\n\ndef hash_password(password):\n    \"\"\"Hashes a password using SHA-256.\"\"\"\n    return hashlib.sha256(password.encode()).hexdigest()\n\ndef check_password(stored_hash, provided_password):\n    \"\"\"Checks if the provided password matches the stored hash.\"\"\"\n    return hash_password(provided_password) == stored_hash",
  "q_06": "from sqlalchemy.orm import Session\n\ndef get_db():\n    \"\"\"Provides a context manager for managing database sessions.\"\"\"\n    db = Session()\n    try:\n        yield db\n    finally:\n        db.close()\n\nThis function uses SQLAlchemy's ORM to manage database sessions. It provides a context manager that ensures each session is properly closed after use, even if an error occurs within the block.",
  "q_07": "def create_db_connection_string(settings):\n    \"\"\"\n    Creates a connection string for PostgreSQL from application settings.\n    \n    :param settings: Dictionary containing database settings with keys 'host', 'port', 'user', 'password', and 'dbname'.\n    :return: A string representing the PostgreSQL connection URL.\n    \"\"\"\n    return f\"postgresql://{settings['user']}:{settings['password']}@{settings['host']}:{settings['port']}/{settings['dbname']}\"",
  "q_08": "import os\n\nclass ConfigLoader:\n    \"\"\"Load configuration based on the runtime environment.\"\"\"\n    \n    def __init__(self):\n        self.config = {}\n    \n    def load_config(self, env: str) -> None:\n        config_file = f'config.{env}.yaml'\n        if not os.path.exists(config_file):\n            raise FileNotFoundError(f\"Configuration file {config_file} not found.\")\n        \n        with open(config_file, 'r') as file:\n            self.config = yaml.safe_load(file)\n    \n    def get_config(self, key: str) -> any:\n        return self.config.get(key, None)\n\nThis class `ConfigLoader` loads configuration settings from a YAML file based on the runtime environment. The `load_config` method reads the appropriate configuration file and stores it in an instance variable. The `get_config` method retrieves a specific configuration value by key.",
  "q_09": "class BaseRepository:\n    def create_record(self, owner_id, data):\n        \"\"\"\n        Create a new record associated with the given owner.\n        \"\"\"\n        # Simulate database insertion\n        record = {\n            'id': self.generate_id(),\n            'owner_id': owner_id,\n            **data\n        }\n        self.save_to_database(record)\n        return record\n\n    def generate_id(self):\n        \"\"\"\n        Generate a unique ID for a new record.\n        \"\"\"\n        import uuid\n        return str(uuid.uuid4())\n\n    def save_to_database(self, record):\n        \"\"\"\n        Save the record to the database.\n        \"\"\"\n        # Simulate saving to a database\n        print(f\"Saving record: {record}\")\n\nThis class provides a basic structure for creating records with an owner. It includes methods to generate a unique ID and save the record to a simulated database.",
  "q_10": "def handle_invalid_credentials():\n    \"\"\"\n    Returns an error response for invalid credentials with a 401 Unauthorized status code.\n    \"\"\"\n    return {\n        \"error\": \"Invalid credentials\",\n        \"status_code\": 401\n    }",
  "q_11": "def is_training_plan_unique(plan_id: int) -> bool:\n    \"\"\"Check if the training plan with the given ID already exists.\"\"\"\n    return not TrainingPlan.objects.filter(id=plan_id).exists()",
  "q_12": "class TrainingPlan:\n    def __init__(self):\n        self.units = set()\n\n    def add_unit(self, unit):\n        if unit in self.units:\n            raise ValueError(\"Unit already exists in the plan\")\n        self.units.add(unit)\n\nThis class `TrainingPlan` uses a set to store unique training units. The `add_unit` method checks if the unit is already in the set before adding it, raising a `ValueError` if it is.",
  "q_13": "class ExerciseSchema:\n    \"\"\"Schema for creating an exercise with required fields and value constraints.\"\"\"\n\n    def __init__(self, name: str, description: str, difficulty_level: int):\n        self.name = name  # Required, must be a non-empty string\n        self.description = description  # Required, must be a non-empty string\n        self.difficulty_level = difficulty_level  # Required, must be an integer between 1 and 5\n\n    @property\n    def is_valid(self) -> bool:\n        return (\n            isinstance(self.name, str)\n            and len(self.name.strip()) > 0\n            and isinstance(self.description, str)\n            and len(self.description.strip()) > 0\n            and isinstance(self.difficulty_level, int)\n            and 1 <= self.difficulty_level <= 5\n        )",
  "q_14": "class TrainingPlanManager:\n    def __init__(self, user):\n        self.user = user\n\n    def can_delete(self, plan):\n        \"\"\"\n        Determine if the current user can delete the given training plan.\n        \"\"\"\n        return self.user.is_superuser or plan.owner == self.user",
  "q_15": "class Paginator:\n    \"\"\"Class for handling pagination of database queries.\"\"\"\n    \n    def __init__(self, page=1, per_page=10):\n        self.page = max(1, int(page))\n        self.per_page = max(1, int(per_page))\n\n    def get_offset(self):\n        return (self.page - 1) * self.per_page\n\n    def apply_to_query(self, query):\n        offset = self.get_offset()\n        return query.limit(self.per_page).offset(offset)",
  "g_01": "class ExerciseData:\n    \"\"\"Represents the structure of training exercise data.\"\"\"\n    \n    def __init__(self, name: str, description: str, muscles_worked: list):\n        self.name = name\n        self.description = description\n        self.muscles_worked = muscles_worked\n\n    def to_dict(self) -> dict:\n        return {\n            'name': self.name,\n            'description': self.description,\n            'muscles_worked': self.muscles_worked\n        }",
  "g_02": "class BodyPartFetcher:\n    def __init__(self, api_client):\n        self.api_client = api_client\n\n    def fetch_body_parts(self, page_number=1, page_size=20):\n        \"\"\"\n        Fetches body parts with pagination.\n\n        :param page_number: The current page number.\n        :param page_size: Number of items per page.\n        :return: A list of body parts.\n        \"\"\"\n        response = self.api_client.get('/body-parts', params={'page': page_number, 'size': page_size})\n        return response.json()['body_parts']",
  "g_03": "class DatabaseConnection:\n    \"\"\"Base class for managing database connections.\"\"\"\n    def connect(self):\n        raise NotImplementedError\n\n    def disconnect(self):\n        raise NotImplementedError\n\n\nclass SQLDatabase(DatabaseConnection):\n    \"\"\"Class for interacting with SQL databases.\"\"\"\n    def connect(self):\n        # Implementation to connect to an SQL database\n        pass\n\n    def disconnect(self):\n        # Implementation to disconnect from an SQL database\n        pass\n\n\nclass NoSQLDatabase(DatabaseConnection):\n    \"\"\"Class for interacting with NoSQL databases.\"\"\"\n    def connect(self):\n        # Implementation to connect to a NoSQL database\n        pass\n\n    def disconnect(self):\n        # Implementation to disconnect from a NoSQL database\n        pass",
  "g_04": "class TrainingUnitSchema:\n    \"\"\"Represents the schema for a training unit.\"\"\"\n    \n    def __init__(self, id, title, description, duration):\n        self.id = id\n        self.title = title\n        self.description = description\n        self.duration = duration",
  "g_05": "class DatabaseConnection:\n    \"\"\"Context manager for managing database connections.\"\"\"\n    \n    def __init__(self, db_url):\n        self.db_url = db_url\n        self.connection = None\n\n    def __enter__(self):\n        import sqlite3\n        self.connection = sqlite3.connect(self.db_url)\n        return self.connection\n\n    def __exit__(self, exc_type, exc_val, exc_tb):\n        if self.connection:\n            self.connection.close()",
  "g_06": "class UserRegistrationSchema:\n    \"\"\"Schema for validating user registration data.\"\"\"\n    \n    def validate(self, data):\n        # Implementation to validate user registration data\n        pass",
  "g_07": "import psycopg2\n\nclass DatabaseConnection:\n    def __init__(self, dbname, user, password, host='localhost', port=5432):\n        self.connection = psycopg2.connect(\n            dbname=dbname,\n            user=user,\n            password=password,\n            host=host,\n            port=port\n        )\n        self.cursor = self.connection.cursor()\n\n    def close(self):\n        self.cursor.close()\n        self.connection.close()",
  "g_08": "class ExerciseFetcher:\n    def fetch_exercises(self, user_id):\n        \"\"\"\n        Fetches exercises for a specific user.\n        \n        Args:\n            user_id (int): The ID of the user whose exercises to fetch.\n            \n        Returns:\n            list: A list of exercise dictionaries.\n        \"\"\"\n        # Simulate fetching exercises from a database\n        return [\n            {'id': 1, 'user_id': user_id, 'title': 'Exercise 1'},\n            {'id': 2, 'user_id': user_id, 'title': 'Exercise 2'}\n        ]",
  "g_09": "class UserService:\n    \"\"\"\n    Service for managing user operations.\n    \"\"\"\n\n    def deactivate_user(self, user_id):\n        \"\"\"\n        Deactivates a user by setting their active status to False.\n\n        :param user_id: ID of the user to deactivate\n        :return: None\n        \"\"\"\n        # Implementation to deactivate the user goes here\n        pass",
  "g_10": "class TrainingUnitPagination:\n    def __init__(self, page_size=10):\n        self.page_size = page_size\n\n    def get_paginated_units(self, total_units, current_page):\n        start_index = (current_page - 1) * self.page_size\n        end_index = start_index + self.page_size\n        return total_units[start_index:end_index]",
  "g_11": "class WorkoutPlan:\n    def remove_exercise(self, exercise_id):\n        \"\"\"Remove an exercise from the workout plan.\"\"\"\n        self.exercises = [e for e in self.exercises if e.id != exercise_id]",
  "g_12": "class ExerciseTypeService:\n    def get_all_exercise_types(self, page=1, per_page=20):\n        \"\"\"\n        Fetches all exercise types with pagination.\n        \n        Args:\n            page (int): The current page number.\n            per_page (int): Number of items per page.\n            \n        Returns:\n            list: A list of exercise type objects for the specified page.\n        \"\"\"\n        offset = (page - 1) * per_page\n        return ExerciseType.query.offset(offset).limit(per_page).all()",
  "g_13": "def find_part_body_by_name(part_name):\n    \"\"\"\n    Find the body of a part by its name.\n    \"\"\"\n    # Implementation goes here\n    pass",
  "g_14": "class LevelService:\n    def get_level_by_id(self, level_id):\n        \"\"\"\n        Retrieve a specific level by its ID.\n        \n        Args:\n            level_id (int): The ID of the level to retrieve.\n            \n        Returns:\n            dict: The level data if found, otherwise None.\n        \"\"\"\n        # Simulate fetching level from database\n        levels = {\n            1: {'id': 1, 'name': 'Easy', 'description': 'Beginner level'},\n            2: {'id': 2, 'name': 'Medium', 'description': 'Intermediate level'},\n            3: {'id': 3, 'name': 'Hard', 'description': 'Advanced level'}\n        }\n        return levels.get(level_id)",
  "g_15": "class DevelopmentConfig:\n    \"\"\"Configuration for development environment.\"\"\"\n    DEBUG = True\n    DATABASE_URI = 'sqlite:///dev.db'\n    SECRET_KEY = 'dev_secret_key'\n\nThis class defines the configuration settings for a development environment in a Python web application. It includes debug mode, database URI, and a secret key.",
  "g_16": "class LevelFetcher:\n    def __init__(self, api_client):\n        self.api_client = api_client\n\n    def fetch_all_levels(self, page_size=10, page_number=1):\n        \"\"\"\n        Fetches all levels with pagination.\n        \"\"\"\n        while True:\n            response = self.api_client.get_levels(page_size=page_size, page_number=page_number)\n            if not response.levels:\n                break\n            for level in response.levels:\n                yield level\n            page_number += 1",
  "g_17": "class ExercisePagination:\n    def __init__(self, page_size=10):\n        self.page_size = page_size\n\n    def paginate_exercises(self, exercises):\n        \"\"\"\n        Paginate a list of exercises.\n        \"\"\"\n        return [exercises[i:i + self.page_size] for i in range(0, len(exercises), self.page_size)]",
  "g_18": "class LevelRepository:\n    def get_level_by_name(self, level_name):\n        \"\"\"\n        Fetches a level by its name from the database.\n        \n        :param level_name: The name of the level to fetch.\n        :return: A dictionary representing the level or None if not found.\n        \"\"\"\n        # Simulated database query\n        levels = {\n            \"easy\": {\"id\": 1, \"name\": \"Easy\", \"difficulty\": \"low\"},\n            \"medium\": {\"id\": 2, \"name\": \"Medium\", \"difficulty\": \"medium\"},\n            \"hard\": {\"id\": 3, \"name\": \"Hard\", \"difficulty\": \"high\"}\n        }\n        return levels.get(level_name.lower())",
  "g_19": "def get_all_users(page, page_size):\n    \"\"\"\n    Retrieve all users with pagination.\n\n    :param page: Current page number (1-based)\n    :param page_size: Number of items per page\n    :return: List of user objects for the specified page\n    \"\"\"\n    offset = (page - 1) * page_size\n    return User.query.limit(page_size).offset(offset).all()",
  "g_20": "def remove_exercise_from_training_unit(training_unit_id, exercise_id):\n    \"\"\"\n    Removes an exercise from a training unit.\n    \n    Args:\n        training_unit_id (int): The ID of the training unit.\n        exercise_id (int): The ID of the exercise to be removed.\n        \n    Returns:\n        bool: True if the exercise was successfully removed, False otherwise.\n    \"\"\"\n    # Assuming there is a database session and models defined\n    from models import TrainingUnit, Exercise\n    \n    try:\n        with db.session.begin():\n            training_unit = TrainingUnit.query.get(training_unit_id)\n            if not training_unit:\n                return False\n            \n            exercise = Exercise.query.get(exercise_id)\n            if not exercise:\n                return False\n            \n            training_unit.exercises.remove(exercise)\n            db.session.delete(exercise)\n        \n        return True\n    except Exception as e:\n        db.session.rollback()\n        print(f\"Error removing exercise: {e}\")\n        return False",
  "g_21": "def update_exercise_type(exercise_id, new_type):\n    \"\"\"\n    Updates the type of an exercise with the given ID.\n    \"\"\"\n    # Implementation goes here\n    pass",
  "g_22": "class TestSettingsConfig:\n    \"\"\"Configure settings for testing.\"\"\"\n    \n    def __init__(self):\n        self.DEBUG = False\n        self.DATABASES = {\n            'default': {\n                'ENGINE': 'django.db.backends.sqlite3',\n                'NAME': ':memory:',\n            }\n        }\n        self.CACHES = {\n            'default': {\n                'BACKEND': 'django.core.cache.backends.locmem.LocMemCache',\n            }\n        }",
  "g_23": "class ExerciseService:\n    def get_exercises_for_workout(self, workout_id):\n        \"\"\"Retrieve all exercises for a specific workout.\"\"\"\n        # Implementation details here\n        pass",
  "g_24": "class ExerciseTypeManager:\n    def delete_exercise_type(self, exercise_type_id):\n        \"\"\"\n        Deletes an exercise type by its ID.\n        \"\"\"\n        # Assuming there's a database session and an ExerciseType model\n        session = get_db_session()\n        exercise_type = session.query(ExerciseType).filter_by(id=exercise_type_id).first()\n        if exercise_type:\n            session.delete(exercise_type)\n            session.commit()",
  "g_25": "def find_exercise_by_name(exercise_name):\n    \"\"\"\n    Find an exercise by its name.\n    \"\"\"\n    # Assuming exercises is a list of dictionaries with 'name' key\n    for exercise in exercises:\n        if exercise['name'] == exercise_name:\n            return exercise\n    return None",
  "g_26": "def is_exercise_in_training_unit(exercise_id, training_units):\n    \"\"\"\n    Check if an exercise belongs to any of the given training units.\n\n    :param exercise_id: ID of the exercise to check.\n    :param training_units: List of training unit objects or IDs.\n    :return: True if the exercise belongs to a training unit, False otherwise.\n    \"\"\"\n    return any(exercise_id in unit.exercises for unit in training_units)",
  "g_27": "def get_owner_training_units(owner_id, page=1, per_page=10):\n    \"\"\"\n    Retrieve all training units for a given owner with pagination.\n    \"\"\"\n    offset = (page - 1) * per_page\n    return TrainingUnit.objects.filter(owner_id=owner_id).order_by('id')[offset:offset + per_page]",
  "g_28": "class TrainingPlan:\n    \"\"\"Represents a training plan with various attributes.\"\"\"\n    \n    def __init__(self, name, duration, exercises):\n        self.name = name\n        self.duration = duration  # in hours\n        self.exercises = exercises  # list of exercise dictionaries\n\n    def add_exercise(self, exercise):\n        self.exercises.append(exercise)\n\n    def remove_exercise(self, exercise_name):\n        self.exercises = [e for e in self.exercises if e['name'] != exercise_name]",
  "g_29": "class TrainingPlanAccessChecker:\n    \"\"\"\n    Checks if the user has access to a training plan.\n    \"\"\"\n\n    def __init__(self, user_id, plan_id):\n        self.user_id = user_id\n        self.plan_id = plan_id\n\n    def has_access(self):\n        # Logic to check if the user has access to the training plan\n        return self._is_user_owner() or self._has_subscription()\n\n    def _is_user_owner(self):\n        # Check if the user is the owner of the training plan\n        return User.objects.get(id=self.user_id).id == TrainingPlan.objects.get(id=self.plan_id).owner_id\n\n    def _has_subscription(self):\n        # Check if the user has a subscription that allows access to the training plan\n        return Subscription.objects.filter(user_id=self.user_id, plan_id=self.plan_id).exists()",
  "g_30": "import logging\n\ndef setup_logger(name):\n    \"\"\"\n    Configure a logger with a file handler and a console handler.\n\n    Args:\n        name (str): The name of the logger.\n\n    Returns:\n        logging.Logger: A configured logger.\n    \"\"\"\n    logger = logging.getLogger(name)\n    logger.setLevel(logging.DEBUG)\n\n    # Create file handler which logs even debug messages\n    fh = logging.FileHandler('app.log')\n    fh.setLevel(logging.DEBUG)\n\n    # Create console handler with a higher log level\n    ch = logging.StreamHandler()\n    ch.setLevel(logging.ERROR)\n\n    # Create formatter and add it to the handlers\n    formatter = logging.Formatter('%(asctime)s - %(name)s - %(levelname)s - %(message)s')\n    fh.setFormatter(formatter)\n    ch.setFormatter(formatter)\n\n    # Add the handlers to the logger\n    logger.addHandler(fh)\n    logger.addHandler(ch)\n\n    return logger",
  "g_31": "class ExerciseStructure:\n    \"\"\"Represents the structure of exercise data in a project.\"\"\"\n    \n    def __init__(self, exercises):\n        self.exercises = exercises  # List of exercise dictionaries\n\n    def add_exercise(self, exercise):\n        \"\"\"Add an exercise to the structure.\"\"\"\n        self.exercises.append(exercise)\n\n    def get_exercise_by_id(self, exercise_id):\n        \"\"\"Retrieve an exercise by its ID.\"\"\"\n        return next((e for e in self.exercises if e['id'] == exercise_id), None)",
  "g_32": "class BodyPart:\n    \"\"\"Represents a body part with its name and description.\"\"\"\n    \n    def __init__(self, name, description):\n        self.name = name\n        self.description = description\n\n# Example usage:\nhead = BodyPart(\"Head\", \"The upper part of the human body containing the brain.\")",
  "g_33": "class TrainingPlanService:\n    \"\"\"\n    Service class for managing training plans.\n    \"\"\"\n\n    def get_all_training_plans(self, user_id):\n        \"\"\"\n        Retrieves all training plans for a given user.\n\n        :param user_id: ID of the user whose training plans are to be retrieved.\n        :return: List of training plans.\n        \"\"\"\n        # Implementation goes here\n        pass",
  "g_34": "class TrainingPlanService:\n    def get_all_training_plans(self, page_number, page_size):\n        \"\"\"\n        Retrieve all training plans with pagination.\n        \n        :param page_number: The current page number (1-based).\n        :param page_size: The number of items per page.\n        :return: A list of training plans for the specified page.\n        \"\"\"\n        offset = (page_number - 1) * page_size\n        return TrainingPlan.objects.all()[offset:offset + page_size]",
  "g_35": "class JwtTokenData:\n    \"\"\"Represents the structure of data in a JWT token.\"\"\"\n    \n    def __init__(self, user_id: int, username: str, roles: list):\n        self.user_id = user_id\n        self.username = username\n        self.roles = roles",
  "g_36": "class TrainingUnit:\n    def add_existing_exercise(self, exercise_id):\n        \"\"\"\n        Adds an existing exercise to this training unit.\n        \"\"\"\n        self.exercises.append(exercise_id)",
  "g_37": "class LevelCreator:\n    def create_level(self, level_data):\n        \"\"\"\n        Create a new level based on the provided data.\n        Raises ValueError if the level data is invalid.\n        \"\"\"\n        try:\n            # Logic to validate and create the level\n            pass\n        except Exception as e:\n            raise ValueError(f\"Failed to create level: {e}\")",
  "g_38": "import bcrypt\n\nclass PasswordManager:\n    \"\"\"\n    Manages password hashing and verification.\n    \"\"\"\n\n    @staticmethod\n    def hash_password(password: str) -> bytes:\n        \"\"\"Hashes a plain text password.\"\"\"\n        return bcrypt.hashpw(password.encode('utf-8'), bcrypt.gensalt())\n\n    @staticmethod\n    def verify_password(stored_password: bytes, provided_password: str) -> bool:\n        \"\"\"Verifies if the provided password matches the stored hash.\"\"\"\n        return bcrypt.checkpw(provided_password.encode('utf-8'), stored_password)",
  "g_39": "class DatabaseManager:\n    def create_record(self, data):\n        \"\"\"Create a new record in the database.\"\"\"\n        # Implementation details here\n        pass",
  "g_40": "class UserTrainingPlan:\n    \"\"\"Represents the schema for a user's training plan.\"\"\"\n    \n    def __init__(self, user_id, exercises, duration):\n        self.user_id = user_id\n        self.exercises = exercises\n        self.duration = duration\n\n    @classmethod\n    def from_dict(cls, data):\n        return cls(\n            user_id=data['user_id'],\n            exercises=data['exercises'],\n            duration=data['duration']\n        )",
  "g_41": "class ExerciseManager:\n    def __init__(self, exercise_store):\n        self.exercise_store = exercise_store\n\n    def get_exercises_for_block(self, block_id):\n        \"\"\"\n        Retrieve exercises associated with a specific training block.\n        \"\"\"\n        return self.exercise_store.get_exercises_by_block(block_id)",
  "g_42": "class UserSchema:\n    \"\"\"Schema for user data.\"\"\"\n    def __init__(self):\n        self.fields = {\n            'id': int,\n            'username': str,\n            'email': str,\n            'password_hash': str,\n            'created_at': datetime.datetime\n        }\n\nThis class defines a schema for user data with fields such as `id`, `username`, `email`, `password_hash`, and `created_at`.",
  "g_43": "class LocalTestSettings:\n    \"\"\"Configuration for local testing.\"\"\"\n    \n    DATABASE_URL = \"sqlite:///test.db\"\n    DEBUG = True\n    SECRET_KEY = 'dev_secret_key'",
  "g_44": "class TrainingPlanService:\n    def get_training_plan_by_id(self, user_id, plan_id):\n        \"\"\"\n        Retrieves a training plan by its ID, considering user privileges.\n        \"\"\"\n        # Assume db is an instance of a database access object\n        plan = self.db.get_training_plan(plan_id)\n        \n        if not plan:\n            raise ValueError(\"Training plan not found\")\n        \n        if not self.has_access(user_id, plan):\n            raise PermissionError(\"Access denied to training plan\")\n        \n        return plan\n\n    def has_access(self, user_id, plan):\n        \"\"\"\n        Checks if the user has access to the training plan.\n        \"\"\"\n        # Assume there's a method to check user permissions\n        return self.db.user_has_permission(user_id, \"view_training_plan\", plan.id)",
  "g_45": "def find_user_by_id(user_id):\n    \"\"\"\n    Finds user by their unique identifier.\n    \"\"\"\n    # Assuming users is a dictionary where keys are user IDs and values are user objects\n    return users.get(user_id, None)",
  "g_46": "class ExerciseSchema:\n    \"\"\"Schema for defining an exercise.\"\"\"\n    \n    def __init__(self, id, name, description):\n        self.id = id\n        self.name = name\n        self.description = description",
  "g_47": "class AuthTokenGenerator:\n    \"\"\"\n    Generates access tokens for authenticated users.\n    \"\"\"\n\n    def generate_token(self, user_id: int) -> str:\n        # Logic to generate a token based on the user ID\n        return f\"token_{user_id}\"\n\nThis class provides a method `generate_token` that takes a user ID and returns an access token. The actual logic for generating tokens would be more complex in a real-world application, likely involving cryptographic hashing or JWTs.",
  "g_48": "def remove_training_unit(plan_id, unit_id):\n    \"\"\"\n    Removes a training unit from a training plan if it exists.\n    \n    Args:\n        plan_id (int): The ID of the training plan.\n        unit_id (int): The ID of the training unit to remove.\n        \n    Returns:\n        bool: True if the unit was removed, False if not found.\n    \"\"\"\n    try:\n        plan = TrainingPlan.objects.get(id=plan_id)\n        unit = plan.units.get(id=unit_id)\n        unit.delete()\n        return True\n    except (TrainingPlan.DoesNotExist, TrainingUnit.DoesNotExist):\n        return False",
  "g_49": "def find_user_by_email(email):\n    \"\"\"\n    Find a user by their email address.\n    \"\"\"\n    # Implementation details would go here\n    pass",
  "g_50": "class DatabaseUpdater:\n    def update_record(self, table_name, record_id, new_data):\n        \"\"\"\n        Update a record in the specified table.\n        \n        :param table_name: Name of the table to update.\n        :param record_id: ID of the record to update.\n        :param new_data: Dictionary containing the new data for the record.\n        \"\"\"\n        # Simulate database update\n        print(f\"Updating {table_name} with ID {record_id}: {new_data}\")",
  "g_51": "class RecordManager:\n    \"\"\"\n    Manages record retrieval by owner.\n    \"\"\"\n\n    def get_records_by_owner(self, owner_id):\n        \"\"\"\n        Retrieves all records owned by the given owner.\n\n        :param owner_id: ID of the owner whose records to retrieve.\n        :return: List of records belonging to the owner.\n        \"\"\"\n        # Implementation details would go here\n        pass",
  "g_52": "class TrainingUnit:\n    def __init__(self):\n        self.exercises = []\n\n    def add_exercise(self, exercise):\n        \"\"\"Add an exercise to the training unit.\"\"\"\n        self.exercises.append(exercise)\n\n    def remove_exercise(self, exercise):\n        \"\"\"Remove an exercise from the training unit if it exists.\"\"\"\n        if exercise in self.exercises:\n            self.exercises.remove(exercise)",
  "g_53": "class AccessToken:\n    \"\"\"Represents an access token with its properties.\"\"\"\n    \n    def __init__(self, token: str, expires_at: datetime.datetime):\n        self.token = token\n        self.expires_at = expires_at\n\n    @property\n    def is_expired(self) -> bool:\n        return datetime.datetime.now() > self.expires_at",
  "g_54": "class ExerciseTypeSchema:\n    \"\"\"Schema for defining exercise types.\"\"\"\n    \n    def __init__(self):\n        self.exercise_types = {\n            \"pushups\": {\"description\": \"Upper body strength training\", \"reps\": 10},\n            \"squats\": {\"description\": \"Lower body strength training\", \"reps\": 15}\n        }",
  "g_55": "class TrainingPlanUpdateHandler:\n    \"\"\"\n    Handles the update of a training plan with access checks.\n    \"\"\"\n\n    def update_training_plan(self, user_id, plan_id, new_data):\n        if not self.has_access(user_id, plan_id):\n            raise PermissionError(\"User does not have permission to update this training plan.\")\n        \n        # Logic to update the training plan\n        pass\n\n    def has_access(self, user_id, plan_id):\n        # Check if the user has the necessary permissions to update the plan\n        return self.user_has_role(user_id, 'admin') or self.user_owns_plan(user_id, plan_id)\n\n    def user_has_role(self, user_id, role):\n        # Logic to check if the user has a specific role\n        pass\n\n    def user_owns_plan(self, user_id, plan_id):\n        # Logic to check if the user owns the training plan\n        pass",
  "g_56": "class ExerciseTypeSchema:\n    \"\"\"Schema for defining exercise types.\"\"\"\n    \n    def __init__(self):\n        self.types = {\n            \"pushups\": {\"description\": \"Upper body strength training.\"},\n            \"squats\": {\"description\": \"Lower body strength training.\"},\n            \"jumps\": {\"description\": \"Cardio and agility training.\"}\n        }"
}